## 2026-06-02 - development-lead gains an explicit design stage (drill finding #5)

Target:
- The successful agent-proxy re-drill built the slice but the lead never wrote the refined design back to DESIGN.md (it embedded decisions in subtask specs). Fix at the right layer: this is a WORKFLOW-shape concern, not an engine/prompt one.

Change:
- `development-lead` now has stages `design → plan → delegate → review → done` (was `plan → delegate → review → done`). The new `design` stage (entry: always) instructs the lead to read + refine the project design doc IN PLACE (add a Decisions section) and record key decisions in a new `design` field; `plan`'s entry guard now requires `design` to exist, so design review is a gated, un-skippable first step.
- Caveat (inherent): wakespace can't verify the file was written (that would be coding-ACI, removed in ADR 0007) — it gates on the `design` field + a dedicated stage with explicit instructions. That's the coordination layer's reach; hard verification would be the verify-stage agent's job (e.g. `git diff DESIGN.md`), not the engine's.

Verification:
- typecheck green; tests green (agent-loop 47, wakespace 121). Updated the 5 development-lead tests to drive the new design stage.

Drift: closer (design review is now a first-class step)

Next adjustment:
- Re-drill agent-proxy and confirm the lead updates DESIGN.md.

## 2026-06-02 - sibling task dependencies (dependsOn) + idempotent create_subtask (ADR 0011)

Target:
- The agent-proxy drill fanned out a layered effort (proxy → capture → API → CLI → rules) as flat parallel subtasks that collided (two both created cmd/*/main.go). The work is a dependency DAG; wakespace had no sibling/cross-task ordering (only parent→child childrenDone). Add it.

Design (answered "should parent→child reuse this model?"): keep childrenDone (parent waits for its dynamic children, gates stage advance) and dependsOn (a task waits for explicit prerequisites, gates task start) as DISTINCT surfaces, but share the terminality predicate + the completion-notification path. They compose: dependsOn orders the DAG; childrenDone waits for the whole DAG. (ADR 0011.)

Implementation:
- `create_subtask` + `key` (logical handle) and `dependsOn` (sibling keys). The lead can't reference engine-minted ids, so deps are by key; the engine resolves keys→ids within the delegate pass (and across re-wakes via the subtask.created `key`).
- `Task.dependsOn` (resolved ids). The engine defers a task's wake until all deps are terminal (a missing dep is treated as satisfied to avoid deadlock); a dependent with unmet deps is created un-scheduled and scheduled when its last dep terminates (`scheduleReadyDependents`, generalizing the persist-schedules-parent hook). A defensive gate in runWake also defers (without charging the wake budget) if `run`/`nudge` schedules it early.
- Found + fixed a real pre-existing bug the DAG test exposed: the lead **re-runs the delegate stage** every time a child finishes while childrenDone is unmet → it **re-spawned** the team (infinite loop, capped only by the wake budget). Fix: `create_subtask` is idempotent per (parent, key) — re-delegating with an existing key is a no-op. (A deeper "don't re-run a stage whose transition was already requested" change was rejected for now because it conflicts with multi-round review; idempotency is the local, safe fix. Note: re-running delegate still burns a few no-op lead wakes — a future optimization.)

Verification:
- typecheck green; tests green (agent-loop 47, wakespace 121). New: a dependsOn chain runs strictly in order (A before B) and the dependent carries the resolved prerequisite id; the lead reaches done.

Drift: closer (the lead can express the real DAG; no more parallel-from-empty-base collisions)

Method feedback:
- The DAG unit test surfaced the re-spawn loop that the drill had only hinted at — small deterministic tests catch what big live runs obscure.

Next adjustment:
- Re-drill agent-proxy: the lead should declare layers with key+dependsOn; expect ordered execution + clean integration. Optionally optimize away the no-op delegate re-runs.

## 2026-06-02 - agent-proxy real drill: findings + fixes (#4 GC race, #2 stuck child)

Target:
- Hand a real greenfield project (../agent-proxy, a Go proxy daemon+CLI with a 340-line DESIGN.md) entirely to wakespace: one development-lead task to review+refine the design, plan, phase, delegate a team, integrate. The client (me) only states the goal and reviews.

What happened (the drill is the dogfood):
- wakespace DID deliver: the lead planned, delegated a team, and the team produced ~2300 lines of real Go (cmd/{agent-proxy,agent-proxyd}, daemon/{proxy,ring,events,rules,api,types}, cli/, model/) that builds, vets, and tests green. Preserved on agent-proxy branch drill/first-slice (commit 47ecb6d).
- But the autonomous orchestration did NOT complete, exposing real bugs:
  1. Heavy `implement` wakes blew past 300s (concurrent cold `go build` of goja across worktrees).
  2. A timed-out child stayed in_progress and WEDGED the lead (childrenDone needs all terminal; no staleness/escalation).
  3. `run --task <lead>` doesn't re-wake already-stuck children (resume mis-targets).
  4. GC reclaimed a done child's branch/worktree BEFORE the lead integrated it (keyed on child-liveness; the lead, stuck, never merged) — premature reclamation / work loss.
  5. The lead planned but didn't write the refined design back to DESIGN.md.

Fixes this entry (asked: fix #4 and #2 first, then re-drill):
- #4: `reconcileWorktrees`/`gcWorktrees` now retain `live ∪ {tasks whose parent is live}` (pure `retainedTaskIds`), so a child's branch survives until the parent effort terminates.
- #2: engine staleness circuit-breaker — a CHILD whose wake errors is retried `maxWakeRetries` (default 1) then terminally failed (engine-sourced `cancel`, now terminal in the reducer), unblocking the parent's childrenDone; root tasks unchanged. Plus the `--wake-timeout` CLI flag for heavy builds.
- Deferred: #1 (build-cache warming / per-stage budgets), #3 (resume semantics — largely mooted by #2), #5 (force design write-back).

Verification:
- typecheck green; tests green (agent-loop 47, wakespace 120). New: pure `retainedTaskIds` unit tests; an engine test where a perpetually-failing child auto-fails and the lead still reaches done (not wedged). Fixed the scriptLoop test helper to mirror the real RunHandle contract (result never rejects → no unhandled rejections).

Drift: closer (the orchestration long-chain is more robust; re-drill next)

Method feedback:
- The real drill found failure modes no unit test had — exactly why dogfooding on a real project matters. Each finding mapped to a concrete, testable fix.

Next adjustment:
- Re-drill agent-proxy on a fresh branch with the fixes (warm cache + circuit-breaker should let it complete or fail-soft). Then revisit #1/#5.

## 2026-06-02 - optional per-subtask isolation via git worktree (ADR 0010)

Target:
- Let a lead run parallel subtasks that edit the same code without clobbering each other, as an OPT-IN capability — kept out of wakespace core (isolation is git/dev-specific; ADR 0007 boundary). Important correction from the prior discussion: a worktree is NOT a system sandbox (bypassPermissions bash still isn't jailed); it isolates workers from EACH OTHER (parallel edits) and needs an integration step. OS-level sandboxing is deferred.

Implementation (git stays entirely at the worker boundary; core only gains a generic flag):
- core: `create_subtask` + opaque `isolate?: boolean`; `Task.isolate`; `initTask`/projection/command-tools schema thread it through. The engine never interprets it.
- engine: two optional hooks — `isolateWorkspace(ctx, project)` (resolve the effective project for an isolated wake, e.g. worktree-rooted) and `releaseWorkspace(task, project)` (on terminal; fired once via a `released` set). Isolated tasks route through `isolateWorkspace` before the worker is built; `teamSnapshots`/the `## Team` section mark isolated children + their branch.
- worker boundary: new `worktree.ts` (the only git-aware module) — `ensureWorktree` (per-repo-locked, idempotent, branch `wakespace/<id>` off HEAD, under `<home>/worktrees/<id>`), `releaseWorktree` (done → commit to branch + remove worktree, keep branch; cancelled → drop both; always prune), `gcWorktrees` (reclaim worktrees of non-live tasks + delete merged `wakespace/*` branches). workspace.ts wires the hooks (no-op off git) and exposes `reconcileWorktrees`; the CLI `run` calls it after quiescence so leftovers never accumulate.
- builtin: `development-lead` delegate uses `isolate: true` for same-code pieces; review merges the isolated branches.

Verification:
- `bun run typecheck` green; `bun run test` green (agent-loop 47, wakespace 117). New deterministic engine test (isolate flows to the child + both hooks fire, lead not isolated) + a real-git `worktree.test.ts` (ensure→edit→release commits to branch and removes the worktree; cancelled drops the branch; gc keeps live, reclaims non-live).

Verifier:
- self-check with the CI gate incl. a real temp-git-repo test for the worktree lifecycle.

Drift: closer (parallel-edit isolation is available, opt-in, with controllable cleanup)

Method feedback:
- Caught and corrected my own wrong claim (worktree ≠ bash sandbox) before building — kept the feature honest and scoped to what worktree actually delivers.

Live dogfood + fix (same session):
- Ran a development-lead task with two `isolate:true` children both editing math.ts, lead integrating. Result: both functions landed in main; worktrees were cleaned (empty `worktrees/`, `git worktree list` = main only). BUT the two `wakespace/*` branches lingered — the lead RE-APPLIED the children's edits in one commit instead of `git merge`-ing, so the branches were never git-`--merged` and my merged-only GC kept them → they would accumulate.
- Fix: GC now deletes `wakespace/<id>` branches keyed on TASK LIVENESS (id not in the live set), not on git-merge detection. By GC time (after `run` quiesces) the lead has already integrated however it chose; a spent branch must go regardless. Confirmed on the real leftover: a `run` (GC) deleted both lingering branches, main intact. Added a branch-cleanup assertion to the real-git test.

Next adjustment:
- OS sandbox for bash safety remains the separate, deferred item. Optionally nudge the lead to use real `git merge` (so history shows merges), but cleanup no longer depends on it.

## 2026-06-02 - lead-team dogfood hardening: bash permission + cwd guardrail

Target:
- Two findings from the live lead-team dogfood runs: (1) a real dev subtask needs to run project checks (bash) during verify, which `acceptEdits` would stall on headless; (2) running a write-class task without a scoped project silently edits the current directory (a stray greet.ts landed in the repo when I forgot `project create --root`).

Live dogfood evidence:
- Full `development-lead` run end to end: lead auto-hired (claude-code), delegated 2 `development` subtasks, each auto-hired claude-code worker actually wrote a file (add.ts / sub.ts) jailed to the sandbox project root; lead reviewed and synthesized. Repo stayed clean. This validated 0007+0008+0009 + acceptEdits-edit + jailing.
- A prior run with the builtin `default` project (root ".") edited the repo root — diagnosed as a harness mistake (no `project create --root`), not a wakespace bug; the worker correctly used its project root, which defaulted to cwd.

Implementation:
- (1) `discoveredRoster` now defaults claude-code workers to `permissionMode: "bypassPermissions"` (was acceptEdits). A headless autonomous dev worker can't answer prompts but must edit AND run checks; file tools stay jailed to the project root. (worker.ts)
- (2) `create` warns when a write-class workflow (declares a `workerRole`) targets the current directory, so a team isn't pointed at the wrong place by accident. The behavior ("run wakespace in your project") is correct; the warning makes it explicit. (cli.ts)

Verification:
- `bun run typecheck` green; `bun run test` green (agent-loop 47, wakespace 112). New cli test: a `development` create against the default cwd project warns; a `general` create does not.
- Note: `bunx vitest run <file>` on a file that transitively imports `bun` (config-file.ts `import { YAML } from "bun"`) fails under vitest's node pool — run the package `test` script (bun runtime) instead.

Verifier:
- self-check with the deterministic CI gate + live dogfood evidence for the edit/jail path.

Drift: closer (coding team can edit + run checks; cwd footgun guarded)

Method feedback:
- Each live dogfood surfaced the exact next fix (jailing OK; bash permission; cwd guardrail) — keep iterating dogfood→fix.

Next adjustment:
- Live-dogfood a dev task whose verify actually runs a command (e.g. a project with a typecheck script) to confirm bypassPermissions unblocks bash; consider a worktree-per-worker isolation so bash can't escape the project.

## 2026-06-02 - lead team: real code edits + multi-round review

Target:
- After the first live dogfood (lead spawned a 2-member team that read files and summarized; lead reviewed and finished), close the two gaps the run exposed: (1) a coding team must be able to actually EDIT files headless; (2) the lead should be able to run another round of work in review, not only finish or block.

Implementation:
- (1) `discoveredRoster` now sets `permissionMode: "acceptEdits"` on auto-discovered claude-code workers. A claude-code worker is jailed to the project root (cwd + allowedPaths); without an edit-permitting mode it can inspect but not change files, so a coding team couldn't code. acceptEdits lets it edit within its sandbox with no interactive prompt (which it could never answer headless). (worker.ts)
- (2) The `development-lead` `review` stage now enables `create_subtask`, and `done` is re-gated on `childrenDone`. So the lead can spawn follow-up subtasks during review WITHOUT setting `summary`; it is re-woken when they finish and reviews again, only closing out once all children (initial + follow-up) are terminal and a summary is set. No new mechanism — reuses childrenDone over all children + parent re-wake. (builtin.ts)

Verification:
- `bun run typecheck` green; `bun run test` green (agent-loop 47, wakespace 111). New engine test: a lead delegates one child, in review spawns a follow-up (no summary), is re-woken after it finishes, then synthesizes and completes — `reviewPasses === 2`, both children done.
- Live dogfood of the real-code path: see below / next entry.

Verifier:
- self-check with the deterministic CI gate; multi-round path exercised end-to-end with mock loops.

Drift: closer (coding team can edit; lead can iterate in review)

Method feedback:
- The first live dogfood surfaced the exact two gaps to fix next (headless edit permission, single-round review) — dogfood-driven iteration working as intended.

Next adjustment:
- Live-dogfood a real code change with the development-lead workflow (children on `development`, claude-code editing under acceptEdits); watch for the next finding (e.g. whether verify-stage bash needs a broader permission mode than acceptEdits).

## 2026-06-02 - a lead task creates and coordinates a team (ADR 0009)

Target:
- The owner's refined, dev-focused ask: select a workflow (e.g. the built-in development workflow) and designate a 负责人 (lead); the lead builds and coordinates a team. Streamline the mechanism; borrow Claude Code's dynamic-workflow orchestration discipline; do not add complexity.

Rationale / borrowed principles:
- A 3-agent design workflow (research the Claude Code dynamic-workflow pattern ∥ map wakespace primitives → synthesize) confirmed the minimal shape. Key borrowed ideas: orchestration lives in durable workflow state/stages, not the lead's context; each worker runs in a fresh, isolated context with one scoped job and a structured return; split deterministic control (when/how-many/order → guards) from model reasoning (what → the worker); lead decides who/when, worker decides how; re-plan by spawning/cancelling between ticks, never by reaching into a running worker.

Implementation (reuse, don't add mechanism):
- A "lead" = an ordinary task on a lead workflow whose stage enables `create_subtask`; the "team" = its child tasks, each auto-staffed (ADR 0008). Coordination reuses `childrenDone` + parent re-wake on child terminal. No new command/guard/event/scheduler.
- The one load-bearing gap closed: `engine.ts` `teamSnapshots(task)` reads each child's status + summary/request from projections and threads it into `buildSystem`, which renders a read-only `## Team` section. Without it a lead was blind to its team.
- New built-in `development-lead` workflow (`builtin.ts`): plan → delegate (`tools` includes `create_subtask`) → review (entry gated on `childrenDone`) → done; `workerRole: "coding"`. Registered in `workspace.ts`. Designation reuses the existing `createTask` `workerId` pin + `selectWorker`.
- Decisions taken: join on `childrenDone` (lead always re-wakes to decide); reuse the `coding` role (no separate `lead` role yet); read child state live from projections (defer ADR 0001's durable child-status mirroring).

Verification:
- `bun run typecheck` green; `bun run test` green (agent-loop 47, wakespace 110). New integration test: a lead plans → delegates a child → waits on childrenDone → re-wakes → sees the `## Team` section with the child's status+summary → reviews → done.

Verifier:
- self-check with the deterministic CI gate; the lead→review→synthesize path is exercised end to end with mock loops (no live LLM).

Drift: closer (the "select workflow + designate lead → lead builds & coordinates a team" loop now works with one read-only prompt enrichment + one workflow def)

Method feedback:
- Mapping the new feature onto existing primitives (subtasks + childrenDone + parent re-wake + staffing) kept the net surface area to a read-only prompt section and a workflow definition — the simplest way to honour "lead creates a team" without a new orchestration engine.

Next adjustment:
- Live-dogfood `create "<multi-part change>" --workflow development-lead` with only a provider key set; confirm the lead spawns a real team, the team auto-hires, and the lead reviews. Consider, only if needed: re-planning rounds in review (re-gated on childrenDone) and ADR 0001's durable child-status mirroring for audit/replay.

## 2026-06-02 - wakespace owns staffing; the client only states/accepts/supervises

Target:
- Make wakespace genuinely usable for agents day-to-day by having the company staff itself. Today the client had to `worker create` + `worker default` + pass `--worker`; that makes the client do the company's hiring — the wrong role. Following the cybernetics framing: who-is-assigned is internal resource allocation (the controller's control law), not an external input. The client should only state the requirement, accept the deliverable, and supervise.

Rationale:
- Dual of ADR 0007. "How to code" is the plant's internals (stays out of wakespace, fixed last iteration). "Who works on a task" is staffing — a coordination function that belongs in the controller. A worker capability tag is staffing metadata (like labeling an engineer "backend"), matched generically in the management layer; the engine never sees it. The only unavoidable external seam: provisioning the workforce (creds) — wakespace can't conjure API keys.

Implementation (ADR 0008):
- `worker.ts`: added `Worker.roles`, pure helpers `defaultRolesForRuntime` (claude-code → ["coding","general"], ai-sdk → ["general"]), `workerHasRole`, a pure `selectWorker(roster, {workerId?,projectDefault?,workspaceDefault?,workerRole?})`, and `discoveredRoster()` that turns environment discovery (usable runtime × configured provider) into concrete workers with provider-default models, ordered coding-capable first.
- `WorkflowDef.workerRole?` (data; engine never reads it); `development` declares `"coding"`.
- `workspace.ts`: roster = explicit workers, else auto-discovered; replaced the `hire` closure with `selectWorker`, reading `ctx.workflow.workerRole`. Still creds-lazy.
- CLI: `--worker`/`worker default`/`worker create` are now optional overrides, not prerequisites; `worker list` shows the effective (registered-or-discovered) roster with roles; the no-worker error guides toward setting a provider key.

Verification:
- `bun run typecheck` green (both packages); `bun run test` green (agent-loop 47, wakespace 109). Added 8 deterministic `selectWorker`/role unit tests; made the "no hireable worker" integration test deterministic by clearing provider keys so the auto-roster is empty.

Verifier:
- self-check with the deterministic CI gate; pure-function staffing policy is unit-tested in isolation.

Drift: closer (client now only states/accepts/supervises; company hires internally)

Method feedback:
- Extracting the assignment policy as a pure `selectWorker` made it unit-testable without live LLM calls — the right seam between management policy and the wiring that resolves loops.

Next adjustment:
- Live-dogfood `create "<small change>"` with only a provider key set (no `worker create`) and confirm wakespace auto-hires and converges. Then start the autonomous lead/PM layer (research+plan+decompose+follow-up) so the client talks only to it.

## 2026-06-02 - coding belongs to the agent; wakespace stays task-agnostic coordination

Target:
- Make wakespace genuinely usable day-to-day by stopping it from reinventing a coding agent inside the coordination layer. The recurring dogfood failure (a cheap ai-sdk worker can't code) was answered, under ADR 0006, by bolting a coding-specific Agent-Computer Interface into the engine/prompts/workflows. That is the wrong layer: a real coding agent (claude-code/codex) already carries its own ACI and does not have this problem.

Rationale (engineering cybernetics):
- wakespace is the controller (a company coordinating a team); each agent is a plant — a black box with a transfer function. A controller acts on the plant's inputs/outputs and corrects via feedback; it does not reach inside the plant to rewire its hands. "The cheap worker can't code" is a bad plant; the correct control response is to select a better plant (hire a coding-capable worker), not to rebuild the plant inside the controller. This restores the boundary the design README already states: everything in wakespace must compile back to an event/command/guard/projection.

Implementation:
- Wrote ADR 0007 (supersedes 0006), marked 0006 Superseded, indexed both, and strengthened `workspace-engine.md` Non-goals to name "task-specific agent tooling / coding ACI" as out of scope.
- Removed from the engine: `runHostCheck`/`HOST_CHECKS` (which had this repo's own test paths hardcoded into the library), the project-write evidence gate, the verify-stage shell-failure gate, raw-`bash` suppression, the `writeFile`-overwrite refusal, the per-tool write counting, and the `requiresProjectWrite` stage flag.
- Introduced a generic `workerTools(ctx, loop)` engine resolver (mirrors `intakeLoop`). The worker's own tools now arrive at the worker boundary and the engine merges them with command tools without knowing what they are. `workspace.ts` supplies `agent-loop` project tools for an ai-sdk worker; a claude-code worker brings its own interface and gets none.
- Simplified the commit fallback to one generic path (commit_stage / block / cancel, with field-type validation); stripped coding vocabulary from prompts; rewrote the built-in development/general workflow instructions to describe the deliverable, not the editing mechanics.
- Kept the genuine, task-agnostic control kernel: stop-the-run on terminal intent, the wake timeout, the runaway backstop, and sanitized run diagnostics. `agent-loop`'s `viewFile`/`insertInFile`/pipefail `bash` stay as reusable agent tools (coding inside the agent).

Verification:
- `bun run typecheck` (agent-loop + wakespace, green); `bun run test` (agent-loop 47, wakespace 101, all green). Removed the coding-ACI engine tests; added a focused `workerTools` resolver test (engine merges worker tools + command tools and stays coding-free).

Verifier:
- self-check with the deterministic CI gate (typecheck + tests); a 7-agent exploration workflow cross-checked the coding-ACI surface against the design docs before the change.

Drift: closer (back onto the documented architecture)

Method feedback:
- When a control system keeps failing, prefer fixing the plant selection (use a capable worker) over expanding the controller. Domain knowledge that doesn't compile to an event/command/guard/projection does not belong in wakespace.

Next adjustment:
- Live-dogfood a development task with a claude-code worker (no injected tools) to confirm the native-coding-agent path is the pleasant default; consider relocating per-run runtime tuning (ai-sdk `toolChoice`) to the worker boundary too.

## 2026-06-01 - line-block insertion edit affordance

Target:
- Reduce read-only implementation dogfood turns by giving workers a safe structured way to add small documentation or test blocks after inspecting line-numbered context

Implementation:
- Added `insertInFile` to project tools. It inserts a line block into an existing file at `before`, `after`, or `end`, supports an optional exact `expected_line` guard for viewed anchors, preserves project-root path safety, and returns structured write facts.
- Counted `insertInFile` as project write evidence in wakespace, updated development prompts to steer workers toward `insertInFile` for line-block additions and `replaceInFile` for exact replacements, and documented the ACI decision in ADR 0006 and the changelog.
- Removed sandboxed raw `bash` from verify-stage project tools when the host-check path is available, after live dogfood showed a worker wrapping a missing `git` command with `echo EXIT:$?` before using `runHostCheck`.

Verification:
- `bunx --bun vitest run packages/agent-loop/src/test/project-tools.test.ts`; `bunx --bun vitest run packages/wakespace/src/engine/engine.test.ts`; `bun run typecheck`; `git diff --check`
- Source CLI dogfood `dogfood-insertinfile-affordance` reached done. Its implement wake used `insertInFile:1`, `viewFile:1`, and `set_field:2`, recorded `projectWrites=1`, then verify used `runHostCheck` for `diff_check`, `build`, `typecheck`, `test`, `project_tools_test`, and `wakespace_engine_test` with zero failed project commands. This live run happened before verify-stage `bash` removal, so the next live verify should confirm the narrower tool surface.

Verifier:
- self-check with deterministic tests plus PM review of live dogfood chronicle evidence

Drift: closer

Method feedback:
- A line-window reader needs a matching line-block edit tool; otherwise low-cost workers can identify an insertion point but still avoid constructing a large exact replacement.

Next adjustment:
- Re-run a verify-stage dogfood task after hiding raw `bash`, then continue improving implementation ACI with compact repo-map/edit-plan context before the next npm publish.

## 2026-06-01 - host verification checks

Target:
- Let dogfood verify stages run real project checks locally without publishing every small ACI iteration or relying on the sandboxed project shell

Implementation:
- Added verify-only `runHostCheck` to wakespace project tools. It runs an allowlisted host command from the project root (`typecheck`, `test`, `build`, `diff_check`, and focused package tests), returns bounded stdout/stderr plus exit status, and avoids putting the absolute project root into the tool result.
- Generalized the verify gate from failed sandbox shell commands to failed project verification commands, so a failed host check rejects verification fields/transitions and forces commit fallback to block with concrete command evidence.
- Updated verify prompts to prefer `runHostCheck` for deterministic project checks while keeping local iteration batched before npm publish.
- Tightened development implementation instructions after live dogfood showed another read-only implementation wake: implementation workers must either make the smallest valid structured edit or call `block` with a concrete no-edit reason before returning.
- Redacted the real project root from host-check stdout/stderr previews after live dogfood showed Vitest output leaking the absolute checkout path into chronicle facts.

Verification:
- `bunx --bun vitest run packages/wakespace/src/engine/engine.test.ts`; `bunx --bun vitest run packages/wakespace/src/workflow/validate.test.ts`; `bun run typecheck`; `bun run test`; `bun run build`; `git diff --check`
- Source CLI dogfood `task_5e971d61-22b8-42a9-bcd3-564ddd1002e8` blocked in implement after 20 project tool calls and 0 writes; that confirmed the host-check slice did not solve read-only implementation behavior by itself.
- Source CLI dogfood `dogfood-host-check-verify` reached verify after PM unblocked and advanced the prefilled implementation; the worker used `runHostCheck` 6 times (`wakespace_engine_test`, `project_tools_test`, `typecheck`, `diff_check`, `build`, `test`) and reached done with `failedProjectCommandCalls=0`.

Verifier:
- self-check with deterministic tests and live dogfood chronicle evidence

Drift: closer

Method feedback:
- The project shell sandbox is useful for scoped inspection, but verification fidelity needs a real host execution path with a narrow command contract. Implementation still needs stronger edit affordances; prompt tightening helps, but repo-map/edit-plan tooling is still the likely next lever.

Next adjustment:
- Continue local dogfood without npm publish; focus next on turning read-only implementation investigation into concrete structured edits or explicit worker-authored blocks.

## 2026-06-01 - add line-window file viewer

Target:
- Improve the coding ACI so dogfood workers can inspect exact code windows and line numbers before making structured edits, instead of repeatedly dumping full files

Implementation:
- Added `viewFile` to `agent-loop` project tools. It returns a bounded, 1-based line-numbered window with truncation facts. Updated the wakespace project-tool prompt to prefer `rg` for discovery and `viewFile` for targeted inspection before `replaceInFile`.
- Source CLI dogfood evidence still showed the worker spending the implementation wake on `bash:20, readFile:4, viewFile:0` with no project writes, so project-write implementation stages now hide raw `bash` when structured project tools are available. This is ACI shaping, not a fixed budget.

Verification:
- `bunx --bun vitest run packages/agent-loop/src/test/project-tools.test.ts packages/wakespace/src/engine/engine.test.ts`; `bun run typecheck`; `bun run test`; `git diff --check`; `bun run build`
- Source CLI dogfood rerun for `dogfood-viewfile-tool`: worker pass used `rg:17`, `viewFile:10`, `readFile:1`, `bash:0`; commit fallback remained a single `block`. The worker still did not write, so the next dogfood improvement should target edit planning rather than publishing this immediately.

Verifier:
- self-check with focused tests

Drift: closer

Method feedback:
- The worker needs affordances that turn search results into edit-ready context. Line-numbered windows are a small ACI step toward that without constraining budget.

Next adjustment:
- Rerun blocked dogfood coding tasks with source CLI and inspect whether workers use `viewFile`/`replaceInFile`; if they still only inspect, improve edit planning or add repo-map context before publishing another npm version.

## 2026-06-01 - keep tool facts for commit fallback

Target:
- Make dogfood verification and blocked-task decisions auditable from observed tool calls instead of model-reconstructed claims

Implementation:
- Added compact sanitized tool argument/result previews to wake diagnostics and `wake.progress` events. Commit fallback now receives those facts in its system prompt and `wake.commit` stores them for PM review.
- Reconciled the stale `dogfood-viewfile-tool` implementation stage as lead after the code was already implemented, then ran worker verification. The task reached done, but the worker's verification text claimed specific test commands/results that were not visible in the old chronicle surface; that exposed the need for factual tool previews.
- Source CLI dogfood for `dogfood-tool-call-facts` then showed the verify worker running commands such as `bun test 2>&1 | tail -60` in a shell environment without `bun`; because the final pipeline segment succeeded, the tool returned `exitCode:0`. Project `bash` now prefixes commands with `set -o pipefail` so those failures remain non-zero.
- Source CLI dogfood for `dogfood-pipefail-verification` confirmed non-zero shell failures are now visible (`pnpm ... | tail` returned `exitCode:127`), but the worker still accepted the verify stage as done. Verify stages now reject verification fields/transitions when failed project shell commands were observed and expose only `block` in commit fallback.

Verification:
- Focused engine tests cover progress previews, commit fallback receiving tool facts with secret-like fields redacted, and verify-stage blocking after failed project shell commands. Project-tool tests cover `false | true` returning non-zero through the `bash` tool.
- Source CLI dogfood `dogfood-verify-failure-gate` reached verify and blocked instead of done: `fallbackPolicy=verification_command_failed`, `failedProjectCommandCalls=12`, allowed commit fallback tools were only `block`.

Verifier:
- self-check with focused tests pending full test run

Drift: closer

Method feedback:
- Counts (`bash:21`) are not enough for PM review. Wakespace needs compact command/output facts so a lead can distinguish real verification from plausible verification prose.

Next adjustment:
- The next dogfood gap is execution environment fidelity: the project `bash` sandbox can inspect files but cannot run `bun`, `node`, or real package binaries, so verification needs either a host-command verification tool or a correctly provisioned runtime.

## 2026-06-01 - stop repeated terminal state tools

Target:
- Make dogfood wakes faster and less noisy by stopping an agent run once a terminal workflow state tool has recorded durable intent, without adding fixed tool-call or step budgets

Implementation:
- Dogfooded task `dogfood-terminal-command-stop`: the DeepSeek worker produced plan/design, then reproduced the failure by timing out in design commit fallback with `commit_stage:17` and blocking implement fallback with `block:20` after 20 project tool calls and 0 writes. Implemented terminal-run cancellation in the wake engine for worker `request_transition`/`block`/`cancel` commands and commit fallback `commit_stage`/`block`/`cancel`, with a pending-stop path for tools invoked synchronously before the run handle is assigned. Added a project-write evidence gate that rejects implementation output fields and transitions until a structured project write succeeds. Fixed agent-loop cancellation cleanup so expected backend-result rejections after cancellation do not surface as run errors.

Verification:
- `bunx --bun vitest run packages/agent-loop/src/test/executor.test.ts packages/wakespace/src/engine/engine.test.ts`; `bun packages/wakespace/src/cli.ts run --task dogfood-terminal-command-stop` with source CLI exited 0, recorded `projectWrites=0`, rejected two ungrounded implementation `set_field` commands, and commit fallback ended with one block command instead of `block:20`.

Verifier:
- self-check with deterministic tests; live dogfood failure used as pre-change evidence

Drift: closer

Method feedback:
- Terminal workflow tools are exit tools. Letting the model continue after a durable terminal command turns one useful command into repeated tool loops and hides the real blocker.

Next adjustment:
- Continue ACI work on turning implementation context gathering into structured project edits; do not publish every small local iteration, batch npm releases after several verified local dogfood improvements.

## 2026-06-01 - remove count-based worker budgets

Target:
- Align dogfood worker execution with the principle that worker budget is model/context capacity, not fixed wakespace tool-call or step-count caps

Implementation:
- Removed the fixed pre-write project tool-call cap, related prompt text, diagnostics fields, and hard-stop behavior. Removed wakespace worker step-cap wiring from the engine, workspace API, and smoke scripts. Kept semantic safeguards: implementation commit fallback still requires successful project write evidence, existing-file `writeFile` overwrites are refused, and `commit_stage` fields remain typed.

Verification:
- Passed `bunx --bun vitest run packages/wakespace/src/engine/engine.test.ts packages/wakespace/src/workspace.test.ts packages/wakespace/src/workflow/validate.test.ts`, `git diff --check`, `bun run typecheck`, `bun run test`, and `bun run build`. Reran `dogfood-viewfile-tool` with the source CLI after removing worker step-cap wiring: the latest wake was no longer budget-cancelled, completed 24 project tool calls, made 0 project writes, and blocked through write-evidence fallback. That leaves an ACI/task-execution problem, not a fixed-budget blocker.

Verifier:
- deterministic checks plus dogfood chronicle review

Drift: closer

Method feedback:
- Tool-call budgets made the ACI look disciplined but blocked realistic DeepSeek coding turns before context gathering completed. Use diagnostics to expose behavior; use context design, repo maps, file viewers, and write-evidence gates to shape behavior.

Next adjustment:
- Improve the coding ACI/task contract so implementation wakes turn gathered context into structured edits instead of silently finishing without project writes.
- Re-run `dogfood-viewfile-tool` after this change; if it still fails, improve context/tool affordances instead of adding count caps.

## 2026-06-01 - wakespace release auth hardening

Target:
- Make the 0.1.5 dogfood baseline publishable and avoid repeating npm auth discovery failures from generated platform package directories

Implementation:
- Published `wakespace@0.1.5` and all seven platform packages after adding a release preflight and explicit temporary npm userconfig mapping from `NPM_TOKEN`. Documented the required release invocation in the wakespace README.

Verification:
- `bun run release:check`; `bun scripts/release.ts --dry-run`; `NPM_TOKEN="$NPM_TOKEN" bun --cwd packages/wakespace scripts/release.ts`; `npm view wakespace version`; `npm install -g wakespace@0.1.5`; installed package shows launcher and darwin-arm64 optional dependency at 0.1.5

Verifier:
- self-check with npm registry and installed CLI evidence

Drift: closer

Method feedback:
- For multi-package npm publishing from generated package folders, pass auth through `NPM_CONFIG_USERCONFIG` explicitly and preflight `npm whoami`; do not depend on npm climbing to the right `.npmrc`.

Next adjustment:
- Continue the repo-map/file-viewer ACI work after removing count-based worker budgets; the installed-0.1.5 dogfood run proved the published baseline works but also showed count caps block realistic implementation turns too early.

## 2026-06-01 - wakespace pre-write exploration budget

Target:
- Stop development workers from spending an entire implementation wake on repeated project reads without attempting a structured edit

Implementation:
- Added ADR 0006 for coding-agent interface guardrails. Added a pre-write project tool budget for stages marked `requiresProjectWrite`; after the limit, project tools reject the rest of the worker pass, cancel the run, and route to commit fallback instead of allowing repeated read retries or late writes. Added deterministic guards against existing-file `writeFile` overwrites and invalid commit field payloads.

Verification:
- bunx --bun vitest run packages/wakespace/src/engine/engine.test.ts packages/wakespace/src/workflow/validate.test.ts; bun run typecheck; live dogfood wake `dogfood-aci-hard-stop` reached `project_write_budget_exhausted` with worker status `cancelled`, `projectWriteCalls=0`, `projectToolCallsRejectedBeforeWrite=1`, and block-only commit fallback

Verifier:
- self-check with focused engine coverage and live dogfood chronicle evidence

Drift: closer

Method feedback:
- Prompt-only guidance was insufficient for DeepSeek dogfood wakes; enforceable tool policy is needed when chronicle evidence shows repeated read-only loops. Mature coding-agent systems treat editing as a designed interface with guardrails, not raw filesystem access.

Next adjustment:
- Build the next ACI slice around compact repo maps and a file viewer/edit surface rather than more prompt-only tweaks

## 2026-06-01 - wakespace dogfood diagnostics

Target:
- Make wakespace dogfood runs easier to diagnose when a worker reads or writes but fails to advance task state

Implementation:
- Added durable wake.diagnostics chronicle entries for worker and commit passes, plus commit fallback data covering allowed state tools, project tool/write evidence, and first-pass text previews

Verification:
- bunx --bun vitest run packages/wakespace/src/engine/engine.test.ts; bun run typecheck; git diff --check; bun run test; bun run build

Verifier:
- self-check with focused engine tests and workspace checks

Drift: closer

Method feedback:
- Dogfood needs manager-visible facts in the project chronicle, not only transient model output or local logs

Next adjustment:
- Use these diagnostics in the next live wakespace dogfood run to tune worker prompts and task descriptions

## 2026-06-01 - wakespace dogfood duplicate block cleanup

Target:
- Remove misleading PM noise exposed by a live wakespace dogfood task

Implementation:
- Coalesced duplicate block/cancel calls in forced commit fallback so repeated model tool calls create one durable state command instead of a follow-up command.rejected entry

Verification:
- bunx --bun vitest run packages/wakespace/src/engine/engine.test.ts; bun run test; bun run typecheck; bun run build; git diff --check

Verifier:
- self-check with focused engine test and full workspace checks

Drift: closer

Method feedback:
- Live dogfood should inspect chronicle errors after every blocked task; error noise can hide the actual worker failure mode

Next adjustment:
- Add a progress/watch view for long wakes, then revisit why implement-stage workers read files but do not edit them

## 2026-06-01 - wakespace dogfood wake step budget

Target:
- Let development workers complete small implementation wakes instead of exhausting the step budget during inspection

Implementation:
- Raised the default workspace maxStepsPerWake from 6 to 12 and added a workspace wiring test that observes the default maxSteps passed into a wake

Verification:
- bunx --bun vitest run packages/wakespace/src/workspace.test.ts packages/wakespace/src/engine/engine.test.ts; bun run typecheck; git diff --check; bun run test; bun run build

Verifier:
- self-check with focused workspace tests and full workspace checks

Drift: closer

Method feedback:
- When a worker reads the right files but never writes, inspect normalized tool counts before changing task wording; the step budget may be the actual limiting mechanism

Next adjustment:
- Re-run the blocked dogfood implementation with the larger budget and decide whether chronicle --text needs richer details beyond summaries

## 2026-06-01 - wakespace inspect wait primitive

Target:
- Let a PM/agent wait for the next durable workspace event without turning run into a streaming watch command

Implementation:
- Added `inspect wait` with task scoping, after-seq baseline, timeout, poll interval, JSON/text output, README/help docs, and a CLI test that waits for a chronicle append from another process

Verification:
- bunx --bun vitest run packages/wakespace/src/cli.test.ts packages/agent-loop/src/test/project-tools.test.ts packages/wakespace/src/engine/engine.test.ts packages/wakespace/src/workspace.test.ts; bun run typecheck; git diff --check; bun run test; bun run build

Verifier:
- self-check with CLI test plus full workspace checks

Drift: closer

Method feedback:
- Progress waiting belongs on the inspect/read side so writers stay simple and other agents can coordinate around durable events

Next adjustment:
- Use `inspect wait` around the next live dogfood run instead of proposing `run --watch`

## 2026-06-01 - wakespace project edit tool

Target:
- Make AI SDK development workers more likely to perform small source edits during dogfood implementation stages

Implementation:
- Added a project-scoped `replaceInFile` exact-replacement tool and counted it as write evidence for wakespace implement-stage fallback policy

Verification:
- bunx --bun vitest run packages/wakespace/src/cli.test.ts packages/agent-loop/src/test/project-tools.test.ts packages/wakespace/src/engine/engine.test.ts packages/wakespace/src/workspace.test.ts; bun run typecheck; git diff --check; bun run test; bun run build

Verifier:
- self-check with project-tool and engine tests

Drift: closer

Method feedback:
- Whole-file write tools are too coarse for agent code edits; exact replacement gives workers a lower-risk editing path while preserving project-root confinement

Next adjustment:
- Re-run a development dogfood task and check whether workers now call `replaceInFile` or `writeFile` before commit

- Live dogfood confirmed replaceInFile can be used for a small project edit.

## 2026-06-01 - wakespace project write diagnostic wording

Target:
- Keep PM diagnostics accurate now that more than one project edit tool can satisfy write evidence

Implementation:
- Renamed wake diagnostic summary wording from `writeFile` to `projectWrites` and updated commit fallback prompt wording from writeFile-specific to generic project write tools

Verification:
- bunx --bun vitest run packages/agent-loop/src/test/project-tools.test.ts packages/wakespace/src/engine/engine.test.ts packages/wakespace/src/cli.test.ts packages/wakespace/src/workspace.test.ts; bun run typecheck; git diff --check; bun run test; bun run build

Verifier:
- self-check with focused engine tests and full workspace checks

Drift: closer

Method feedback:
- When adding a second tool to an evidence category, rename the diagnostic label at the same time or the chronicle becomes technically correct but misleading

Next adjustment:
- Keep inspecting live chronicle summaries after every dogfood run for labels that encode stale implementation details

## 2026-06-01 - wakespace non-advancing command fallback

Target:
- Prevent workers from leaving edited tasks in progress after recording only non-advancing state such as notes

Implementation:
- Project write evidence now counts only successful write-tool results, and commit fallback now also runs when the first worker pass has commands but none of them can commit or advance the current stage

Verification:
- bunx --bun vitest run packages/wakespace/src/engine/engine.test.ts; bun run typecheck; git diff --check; bun run test; bun run build

Verifier:
- self-check with focused engine tests and full workspace checks

Drift: closer

Method feedback:
- A state command is not necessarily a stage commit signal; dogfood diagnostics should distinguish notes from fields, transitions, blocks, cancellations, and subtasks

Next adjustment:
- Re-run the note-only dogfood task after this fix and verify fallback can close it instead of leaving it in progress

## 2026-06-01 - wakespace implement-stage edit guidance

Target:
- Make development workflow implementation wakes more likely to edit once the target is clear

Implementation:
- Tightened DEVELOPMENT_WORKFLOW implement-stage instructions to prefer replaceInFile for localized edits, avoid writeFile overwrites of existing source files, and stop spending the wake only on inspection after the edit target is clear

Verification:
- bunx --bun vitest run packages/wakespace/src/workflow/validate.test.ts packages/wakespace/src/engine/engine.test.ts packages/wakespace/src/workspace.test.ts; bun run typecheck; git diff --check; bun run test; bun run build

Verifier:
- self-check with focused workflow/engine tests and live dogfood evidence

Drift: closer

Method feedback:
- A done general task can still miss exact wording; use reviewer inspection before treating dogfood task completion as acceptance evidence

Next adjustment:
- Re-run the blocked development task after this instruction change and inspect whether it uses replaceInFile instead of read-only loops

## 2026-06-01 - wakespace PM task splitting

Target:
- Give the lead/PM a usable path to decompose broad blocked work into child tasks and accept child results

Implementation:
- Added CLI `create --parent` wiring through intake/createTask and added `submit transition` as a lead command, with CLI coverage for child creation and lead-driven closure

Verification:
- bunx --bun vitest run packages/wakespace/src/cli.test.ts packages/wakespace/src/engine/engine.test.ts packages/wakespace/src/workspace.test.ts; bun run typecheck; git diff --check; bun run test; bun run build

Verifier:
- self-check with focused CLI test and full workspace checks

Drift: closer

Method feedback:
- When a development task is too broad, dogfood should support PM decomposition instead of repeated wakes against the same poorly scoped prompt

Next adjustment:
- Use `create --parent` to split the blocked chronicle text-detail task into a small child implementation task

## 2026-06-01 - agent-loop production readiness audit

Target:
- Confirm whether agent-loop/agent-workspace can sustain real project feature development

Implementation:
- No source edits; repo audit plus deterministic and live smoke verification

Verification:
- bun run typecheck; bun run test; bun run build; CLI durable smoke; ai-sdk live smoke; workspace live wake smoke

Verifier:
- self-check with deterministic command evidence and one live DeepSeek wake

Drift: closer

Method feedback:
- For production-readiness questions, combine design claims with a real tool-calling wake, not only unit tests

Next adjustment:
- Close workspace durability and documentation gaps before treating it as a production multi-agent workspace

## 2026-06-01 - Claude wake schema and workspace runtime context

Target:
- Make Claude Code workspace wakes complete with structured command tool args and bind default workers to project runtime context

Implementation:
- Updated Claude adapter JSON Schema to Zod conversion, auto-allowed in-process tools, project/worker permissionMode, and workspace project cwd/env propagation

Verification:
- zod latest check; Claude SDK tool signature check; bun run typecheck; bun run test; bun run build; git diff --check; Claude Code smoke-wake done

Verifier:
- self-check with deterministic commands and live Claude Code smoke

Drift: closer

Method feedback:
- When Claude SDK tools are MCP-backed, verify both schema shape and permission behavior with a live wake; passing unit tests alone can miss stream closure

Next adjustment:
- Consider integrating vercel-labs/bash-tool for AI SDK code-development workers behind project-root sandboxing

## 2026-06-01 - AI SDK project tools

Target:
- Give ai-sdk workers usable project development tools: bash/read/write, rg/grep, and web fetch/search, scoped to Project.root

Implementation:
- Added createProjectTools in packages/agent-loop/src/tools/project.ts, exported it, injected it for ai-sdk wakes in packages/agent-workspace/src/engine/engine.ts, and documented available project tools in the wake prompt

Verification:
- bun run typecheck; bun run test; bun run build; git diff --check

Verifier:
- self-check with deterministic tests; no independent reviewer in this turn

Drift: closer

Method feedback:
- For AI SDK code-agent parity, use bash-tool with just-bash ReadWriteFs as the project-scoped shell base, then add first-class structured tools for high-signal operations such as rg and web access

Next adjustment:
- Consider caching per-project tool construction if wake startup latency becomes measurable

## 2026-06-01 - DeepSeek project tools live smoke

Target:
- Use real DeepSeek over AI SDK to verify project tool injection and execution

Implementation:
- Ran a temporary WorkflowEngine with aiSdkLoop(deepseek-v4-flash), a temp project root, and a workflow requiring rg/readFile/writeFile before set_field/request_transition

Verification:
- Live smoke returned LIVE_SMOKE_OK; tool calls included rg, bash, readFile, writeFile, set_field, request_transition; final status done; deepseek-smoke.txt contained VERIFIED:<marker>

Verifier:
- self-check with live DeepSeek run and deterministic postconditions

Drift: closer

Method feedback:
- For production confidence, live provider smoke should assert both normalized tool events and filesystem side effects, not only final text

Next adjustment:
- Promote this one-off smoke into a reusable script if repeated before releases

## 2026-06-01 - Workspace CLI and smoke layout

Target:
- Move agent-workspace CLI out of scripts and rename smoke script directory while keeping DeepSeek project-tool smoke reusable

Implementation:
- Moved CLI source to packages/agent-workspace/src/cli.ts, renamed scripts smoke files into packages/agent-workspace/smokes, updated package scripts and tsconfig, fixed rg execa cancellation option to cancelSignal, and tightened the live smoke assertions

Verification:
- bun run typecheck; bun run test; bun run build; bun run smoke:deepseek-tools; git diff --check

Verifier:
- self-check with deterministic tests and live DeepSeek smoke

Drift: closer

Method feedback:
- Reusable live smokes should assert tool results, not just tool start events; this caught the rg cancelSignal bug after directory rework

Next adjustment:
- None

## 2026-06-01 - Rename main package to wakespace

Target:
- Make wakespace the main workspace package name while keeping agent-loop as the execution library

Implementation:
- Renamed package path and package metadata to packages/wakespace, updated CLI/env/default dir naming, docs, lockfile, and DeepSeek smoke prefixes

Verification:
- bun install; bun run typecheck; bun run test; bun run build; git diff --check; zsh -ic 'cd packages/wakespace && bun run smoke:deepseek-tools' returned LIVE_SMOKE_OK; npm pack dry-run excluded tests, but installing the tarball failed with EUNSUPPORTEDPROTOCOL because agent-loop is still workspace:*

Verifier:
- self-check with deterministic commands and live DeepSeek smoke; no independent reviewer in this turn

Drift: closer

Method feedback:
- For a package rename intended for dogfood, verify package-manager resolution, CLI build output, stale-name search, and one live wake together

Next adjustment:
- Resolve publish packaging for wakespace while agent-loop remains unpublished, likely by bundling agent-loop into the release output or otherwise removing workspace:* from the published dependency graph

## 2026-06-01 - Publish wakespace as CLI-only Bun binary

Target:
- Bundle agent-loop into the wakespace CLI executable instead of publishing wakespace as a library or copying workspace packages

Implementation:
- Changed packages/wakespace/package.json to CLI-only bin=dist/wakespace with no module/types/exports/dependencies; prepack builds the compiled binary; worker discovery can detect AI SDK providers compiled into the binary; docs now describe wakespace as CLI-only

Verification:
- bun install; bun run typecheck; bun run test; bun run build; npm pack --dry-run includes only README.md, dist/wakespace, package.json; installing the tarball in an empty consumer has no node_modules/agent-loop and wakespace help/status/worker discover works; installed binary completed a real DeepSeek ai-sdk wake with task status done and summary binary smoke ok

Verifier:
- self-check with deterministic commands, isolated npm install, and live DeepSeek binary smoke; no independent reviewer in this turn

Drift: closer

Method feedback:
- For CLI-only npm dogfood, validate the installed tarball binary from an empty consumer rather than reasoning from workspace dependencies

Next adjustment:
- Before first public npm publish, decide platform strategy for Bun compiled binaries or publish only the local dogfood platform initially

## 2026-06-01 - CLI defaults for agents plus human overview

Target:
- Make wakespace CLI agent-friendly by default and add a human-readable projects/tasks overview before npm dogfood

Implementation:
- Agent-facing commands now default to JSON with --text/--human for ad-hoc text; added overview command and workspaceOverview/renderOverview view combining projects, workers, recent tasks, activity, and errors; updated README

Verification:
- bun run typecheck; bun run test; bun run build; git diff --check; npm pack --dry-run shows README.md, dist/wakespace, package.json only; installed tarball in empty consumer has no node_modules/agent-loop; default create/status/task outputs JSON; overview outputs human project/task dashboard; installed binary completed a real DeepSeek ai-sdk wake with status done and summary cli ux binary ok

Verifier:
- self-check with deterministic commands, isolated npm install, and live DeepSeek binary smoke; no independent reviewer in this turn

Drift: closer

Method feedback:
- Default CLI output for agent dogfood should be structured; human-friendly summaries should be explicit commands or flags

Next adjustment:
- Consider adding shell completion or short aliases only after dogfood shows repeated command friction

## 2026-06-01 - wakespace npm release preparation

Target:
- Prepare wakespace for a first CLI-only npm dogfood release without publishing

Implementation:
- Set wakespace to 0.1.0; added package metadata, darwin/arm64 platform guard, LICENSE files, npm bin wrapper around the Bun compiled binary, prepublishOnly/prepack/publish:dry scripts, and root release:check

Verification:
- npm view wakespace@0.1.0 returned 404; bun run release:check passed; npm publish --dry-run produced wakespace@0.1.0 with LICENSE, README.md, bin/wakespace, dist/wakespace, package.json; tarball installed in an empty consumer, bin linked to wrapper, no node_modules/agent-loop, and wakespace status returned JSON; npm whoami returned E401 so real publish still needs npm login

Verifier:
- self-check with deterministic commands, registry checks, npm publish dry-run, and isolated tarball install; no independent reviewer in this turn

Drift: closer

Method feedback:
- For compiled-binary npm CLIs, use a shebang wrapper for the npm bin and keep the platform guard explicit

Next adjustment:
- Run npm login, then cd packages/wakespace && npm publish when ready

## 2026-06-01 - wakespace dogfood project memory and runtime discovery

Target:
- Use wakespace as the project-management surface for its own dogfood work, add markdown project memory, and make worker discovery show Codex/Cursor visibility without suggesting unsupported command-tool workers

Implementation:
- Installed wakespace@0.1.0 globally, initialized `.wakespace` with an `agent-loop` project and `deepseek-sdk` default worker, added `.wakespace/` to `.gitignore`, created wakespace task cards, added `projects/<id>.md` memory loading plus CLI read/write and prompt injection, and added discovery runtime details for codex/cursor

Verification:
- bunx --bun vitest run src/project.test.ts src/worker.test.ts; bun run test; bun run typecheck; bun run build; git diff --check; local CLI project memory and worker discover smoke

Verifier:
- self-check with deterministic commands and local CLI smoke; no independent reviewer in this turn

Drift: closer

Method feedback:
- When dogfooding wakespace from inside its own repo, keep `.wakespace/` ignored and use DeepSeek as the default worker while recording management state in task cards

Next adjustment:
- Add a direct command for lead-driven task transition closure so project-manager updates can close general tasks without invoking a worker wake

## 2026-06-01 - Global wakespace home design

Target:
- Design `~/.wakespace` as the default home for project workspace state, project memory, worktrees, artifacts, and global worker profiles

Implementation:
- Added ADR 0003 with a proposed home-root layout, per-project `project.yaml`/`memory.md`/`state`/`worktrees`/`artifacts` directories, global `workers`, `WAKESPACE_HOME`, and legacy `--dir` compatibility

Verification:
- git diff --check; reviewed ADR content; recorded and closed the design task in local wakespace dogfood state

Verifier:
- self-check with design review and local wakespace task closure; no independent reviewer in this turn

Drift: closer

Method feedback:
- For storage-layout changes, design the resolver boundary first so implementation can move stores without spreading path semantics across CLI and engine code

Next adjustment:
- Implement path-resolution utilities before moving project stores or changing CLI defaults

## 2026-06-01 - Worker command commit dogfood repair

Target:
- Make wakespace dogfood tasks actually close through worker-emitted state commands rather than lead-side manual closure

Implementation:
- Added an AI SDK runtimeOptions pass-through for toolChoice, activeTools, and providerOptions; added a wake commit pass that runs when a worker produces no state commands; exposed commit-only tools (`commit_done`, `block`, `cancel`) for that pass; disabled DeepSeek thinking during required tool-choice commit calls; added regression tests for successful commit and no-command failure

Verification:
- bunx --bun vitest run src/engine/engine.test.ts; bun run typecheck; zsh -ic 'bun packages/wakespace/src/cli.ts run --task worker-execute-dogfood-validation --dir .wakespace --text'; bun run test; bun run build; git diff --check

Verifier:
- self-check with deterministic tests plus a live DeepSeek wakespace task that advanced to done through worker field.set and transition.requested events

Drift: closer

Method feedback:
- Dogfood acceptance must inspect the task timeline for worker-sourced durable events, not just task-card status or lead-authored closure

Next adjustment:
- Preserve worker output text in chronicle or task notes so no-command first passes are diagnosable without rerunning with extra hooks

## 2026-06-01 - Release prep dogfood guardrails

Target:
- Prepare wakespace 0.1.1 release metadata through wakespace worker tasks while preventing false task completion during dogfood

Implementation:
- Bumped wakespace release metadata to 0.1.1 through worker tasks, added the 0.1.1 changelog entry through a worker task using a local `.wakespace` instruction file, required AI SDK wakes to call tools, required writeFile evidence before fallback commit completion can mark done, and made unblock reset the current-stage event window so stale transition requests cannot complete a task

Verification:
- bunx --bun vitest run src/engine/engine.test.ts src/workflow/reducer.test.ts; bun run typecheck; live DeepSeek wakespace release metadata/changelog tasks with PM file-diff review

Verifier:
- self-check with focused deterministic tests plus PM review of worker-produced diffs; no independent reviewer in this turn

Drift: closer

Method feedback:
- For worker-executed file edits, task status is not acceptance; PM review must compare the requested file diff, and long exact content should be passed through a file instead of shell-quoted task text

Next adjustment:
- Add structured acceptance fields or verifier hooks so wakespace can reject incorrect worker diffs without creating follow-up correction tasks

## 2026-06-01 - Development workflow dogfood stages

Target:
- Add a general development workflow for wakespace tasks with explicit plan, design, implement, and verify phases

Implementation:
- Added builtin `development` workflow, registered it ahead of user extras, exported it through the workflow API, and covered the full engine progression with tests

Verification:
- bunx --bun vitest run src/workflow/validate.test.ts src/workspace.test.ts src/engine/engine.test.ts src/index.test.ts; bun run typecheck; bun run test; git diff --check

Verifier:
- self-check with deterministic command evidence; no independent reviewer in this turn

Drift: closer

Method feedback:
- For staged agent workflows, test engine progression with a script loop that emits all required state commands per stage instead of assuming a single mock response can represent multiple tool calls

Next adjustment:
- Dogfood a real development task through the new workflow before tightening stage acceptance rules

## 2026-06-01 - Global wakespace home minimal implementation

Target:
- Move normal wakespace state to a global home layout while keeping explicit store overrides for tests and migration

Implementation:
- Added workspace path resolution for `WAKESPACE_HOME`, legacy `WAKESPACE_DIR`, and `--dir`; moved project config/memory writes to `projects/<id>/project.yaml` and `memory.md`; routed new task events/projections to `projects/<id>/state`; kept legacy flat project/task/chronicle reads during dogfood migration

Verification:
- bunx --bun vitest run src/project.test.ts src/workspace.test.ts src/store/jsonl.test.ts src/index.test.ts; bun run test; bun run typecheck; bun run build; git diff --check; CLI smokes for WAKESPACE_HOME and --dir layouts

Verifier:
- self-check with deterministic commands and CLI filesystem smokes; no independent reviewer in this turn

Drift: closer

Method feedback:
- Dogfood development tasks can still block in plan if the first worker pass emits no state commands; preserve that evidence and fix commit/transcript diagnostics separately instead of mixing it into storage-layout work

Next adjustment:
- Add worktree allocation under the project layout, then make development tasks choose isolated worktrees before project writes

## 2026-06-01 - Development fallback commit policy

Target:
- Fix dogfood development tasks that block in plan/design because the first worker pass produced no state commands and no project writes

Implementation:
- Added stage-level `requiresProjectWrite` and `outputFields`; marked general/open and development/implement as write-evidence-required; changed the no-state-command fallback pass to expose an atomic `commit_stage` tool with current task fields when the stage does not require project writes, and only force block when required write evidence is absent

Verification:
- bunx --bun vitest run src/engine/engine.test.ts src/workflow/validate.test.ts; local wakespace rerun advanced `global-home-minimal` through plan and design via fallback commit, then blocked at implement because no project writeFile evidence was observed; clean local dogfood task `clean-global-home-dogfood-2` reached implement with repo-specific plan/design and preserved request; bun run typecheck; bun run test; git diff --check

Verifier:
- self-check with deterministic regression tests and local dogfood rerun evidence

Drift: closer

Method feedback:
- Fallback completion policy should be explicit workflow data and an atomic commit tool, not inferred from project tool counts or left as multiple independent state-tool calls

Next adjustment:
- Preserve first-pass worker text/tool traces in chronicle so PMs can diagnose why the initial pass missed state tools

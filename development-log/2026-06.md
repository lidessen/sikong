## 2026-06-13 - Adapt copied agent-loop package

Target:

- Make packages/agent-loop work inside the sikong Bun workspace

Implementation:

- Updated root scripts, agent-loop package scripts/dependencies, lint fixes, formatting, and docs

Verification:

- bun run fmt && bun run check && bun run build

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Run copied workspace package checks before editing; root checks should include copied package typecheck and tests

Next adjustment:

- Commit after review if desired

## 2026-06-16 - Move task progression to daemon scheduler

Target:

- Let tasks continue after a Client Agent turn without UI-side direct driving

Implementation:

- Added a Go daemon scheduler that scans durable runnable tasks and invokes one
  workspace CLI `task tick` per runnable task
- Added `task runnable` and `task tick` command surfaces as the scheduler
  contract
- Moved worker observations out of task projection events into per-run
  observation JSONL files, with detail views loading filtered semantic facts
- Replaced client-server auto-drive with daemon scheduler wake calls and exposed
  scheduler state in the UI

Verification:

- pending final `bun run check`

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep scheduling in Go as liveness only; every domain decision still comes from
  TypeScript ticks over durable projections

Next adjustment:

- Run a live multi-task E2E with daemon scheduler enabled after checks pass

## 2026-06-16 - Todo app scheduler E2E

Target:

- Run a fresh UI-submitted development task through daemon scheduler from
  workspace creation to accepted output

Implementation:

- Started an isolated Sikong instance with `SIKONG_DATA_DIR` under `/tmp`
- Submitted a Client Agent turn to create `Todo E2E` and a native HTML/CSS/JS
  todo app task
- Let daemon scheduler advance all lead, planner, worker, review, and final
  acceptance steps without direct `task drive` or manual plan acceptance
- Fixed discovered startup bug where `sikong start --daemon` did not pass the
  selected daemon address to the UI process
- Fixed scheduler status so stale `lastError` is cleared after later successful
  scans/ticks

Verification:

- Task `task_b56d58f2-bb7b-4d31-8394-a0b0142d31fb` completed with accepted
  outcome
- Produced `/tmp/sikong-e2e-todo-20260616-083751/project/index.html`
- Worker-generated verification reported 31/31 E2E checks passed and 17
  screenshots captured
- `bun run check`

Verifier:

- self-check with captured task events, generated artifacts, screenshot files,
  and root check output

Drift: closer

Method feedback:

- Full UI-submitted E2E is now viable; keep watching scheduler observability
  because stale or low-detail errors can mislead the UI even after task success

Next adjustment:

- Add an operator-facing process-run inspection endpoint or UI surface for the
  currently running runtime process

## 2026-06-16 - Start stage work units in parallel

Target:

- Make lead-planned work units inside one stage round run concurrently in the
  daemon-backed product path

Implementation:

- Added `start_stage_workers` as the multi-work-unit orchestration action when
  a round has more than one unstarted work unit
- Kept single-work-unit rounds on the existing `start_stage_worker` action
- Updated the process executor to launch one daemon process per work unit before
  waiting for results, then aggregate the completed worker summaries
- Updated runner serialization, runtime assembly hydration, compact summaries,
  and UI action labels for the grouped action

Verification:

- `bun --filter @sikong/workspace test src/orchestration/orchestration.test.ts`
- pending final `bun run check`

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Treat Promise usage as coordinator plumbing only; product worker parallelism
  should remain daemon-supervised subprocess parallelism

Next adjustment:

- Run a live multi-work-unit task and inspect process-run overlap if more
  scheduler evidence is needed

## 2026-06-16 - Split console UI and expose long-run process facts

Target:

- Reduce oversized client UI components and improve long-running task
  observability

Implementation:

- Split `App.tsx` into focused modules for chat, workspace navigation, settings,
  task detail, shared empty state, and task labels
- Added process-run list filtering by workspace, task, state, and limit
- Loaded task-scoped daemon process snapshots into the task detail view
- Raised scheduler-driven tick defaults to a 2 hour process timeout and 2 hour
  plus 60 second wait timeout, with environment overrides

Verification:

- pending final `bun run check`

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep scheduler timeouts as safety fallbacks; long work should be observable
  through process facts instead of forced into short UI request windows

## 2026-06-13 - Remove placeholder tooling workspace

Target:

- Keep sikong workspaces aligned with real packages after agent-loop import

Implementation:

- Deleted packages/tooling and removed root script/docs references

Verification:

- bun install && bun run fmt && bun run check && bun run build

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- After deleting placeholder workspaces, update root aggregate scripts before verifying

Next adjustment:

- Review and commit local adaptation when ready

## 2026-06-13 - Document agent-loop design

Target:

- Add a maintainer-facing design map for packages/agent-loop based on the current implementation

Implementation:

- Added packages/agent-loop/DESIGN.md covering core contracts, executor flow, providers, adapters, task supervision, project tools, and extension points

Verification:

- bun --filter agent-loop typecheck
- bun --filter agent-loop test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- For copied packages with a detailed README, verify adapter and executor source before writing design docs so the doc reflects implemented boundaries rather than only public API intent

Next adjustment:

- Keep DESIGN.md updated when adding runtime capabilities or changing provider compatibility

## 2026-06-13 - Simplify agent-loop task layer terminal protocol

Target:

- Rework packages/agent-loop task execution into a thin multi-round extension over loop.run with only completed, failed, and budget_exceeded terminal states

Implementation:

- Replaced task handoff stores and stuck/exhausted statuses with terminal tools task_complete, task_fail, and finish-only task_budget_exceeded
- Made report required for every terminal outcome and kept round-to-round state transient inside runTask
- Updated task exports, task tests, README, and DESIGN.md to match the simpler protocol

Verification:

- bun --filter agent-loop typecheck
- bun --filter agent-loop test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep the Ralph-style task layer as a thin run extension: terminal tools own the final state, while budget closure uses a finish-only round instead of durable handoff state

Next adjustment:

- Revisit whether runTask should become a method on AgentLoop after the top-level helper semantics settle

## 2026-06-13 - Add gated continuation protocol to agent-loop tasks

Target:

- Make runTask support multi-loop continuation with a transient timeline and gate review for complete/fail claims

Implementation:

- Added namespaced task tools: agent_loop_task_read, agent_loop_task_continue, agent_loop_task_complete, agent_loop_task_fail, and finish-only agent_loop_task_budget_exceeded
- Added gate tools agent_loop_gate_accept and agent_loop_gate_reject, with gateLoop/gateTools/gateMaxSteps support
- Updated runTask so continue appends timeline state, complete/fail require gate acceptance, gate rejection feeds the next round, and gate runs do not consume work budget
- Updated task exports, task tests, README, and DESIGN.md

Verification:

- bun --filter agent-loop typecheck
- bun --filter agent-loop test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep task continuation explicit through a single continue exit tool, and keep validation separate through gate accept/reject so worker claims do not directly finalize task state

Next adjustment:

- Consider whether task_read should summarize long timelines once reports grow beyond prompt budget

## 2026-06-13 - Scope AI SDK tool bundle naming

Target:

- Rename the optional local tool bundle so it does not imply a cross-backend project abstraction

Implementation:

- Renamed createAiSdkProjectTools/AiSdkProjectToolOptions to createAiSdkTools/AiSdkToolOptions
- Moved the implementation file from src/tools/project.ts to src/tools/ai-sdk.ts and renamed the test file to ai-sdk-tools.test.ts
- Updated docs to state Claude Code, Codex, and Cursor should prefer native tool surfaces, sandbox, approval, and permission mechanisms through their adapters

Verification:

- bun --filter agent-loop typecheck
- bun --filter agent-loop test -- src/test/ai-sdk-tools.test.ts

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Name helper APIs after the runtime boundary they support, not after a broad domain concept like project when adapter-native tools remain preferred

Next adjustment:

- None

## 2026-06-13 - Close agent-loop task run gaps

Target:

- Make runTask behave as a closed, simple extension of loop.run across worker, gate, and finish paths

Implementation:

- Converted gate startup/runtime failures into failed TaskResult values instead of rejected runTask promises
- Preserved accepted gate reports as TaskResult.gateReport
- Passed worker and gate run options through separately, including skills, MCP, effort, runtimeOptions, run hooks, and metadata
- Wired RunInput.metadata through ResolvedRequest so adapters can receive it
- Added regression tests for gate failure handling, gate report retention, and worker/gate option pass-through
- Updated DESIGN.md to document the closed task semantics

Verification:

- bun --filter agent-loop typecheck
- bun --filter agent-loop test -- src/test/task.test.ts
- bun --filter agent-loop test
- bun run fmt:check
- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- When task orchestration claims to be a thin loop.run extension, tests should capture both terminal protocol behavior and pass-through of ordinary run inputs

Next adjustment:

- Consider promoting runTask onto AgentLoop only after the helper API remains stable across backend adapters

## 2026-06-13 - Simplify gate API and add loop.runTask

Target:

- Keep the task layer as a simple loop.run extension with a narrow gate surface and absorbing terminal tools

Implementation:

- Added AgentLoop.runTask(input) as the primary task entrypoint while keeping standalone runTask for fresh loop factories
- Removed gate-specific system, skills, MCP, effort, runtimeOptions, runHooks, and metadata overrides
- Kept only gateLoop, gateTools, and gateMaxSteps as gate configuration, with gateMaxSteps defaulting to 50
- Made first terminal task/gate outcome win and wrapped caller tools so custom tool calls after terminal are skipped
- Documented backend terminal-stop mechanisms for AI SDK, Claude Code, Codex, Cursor, and mock

Verification:

- bun --filter agent-loop typecheck
- bun --filter agent-loop test -- src/test/task.test.ts

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Gate review should stay narrow: keep reviewer-specific behavior in the gate prompt and backend/tool policy, not in a parallel set of run options

Next adjustment:

- Use backend sandbox or permission policy when strict prevention of already-scheduled native parallel tool calls is required

## 2026-06-13 - Document Sikong project shape

Target:

- Record the intended repository and package shape before redesigning the multi-worker coordination engine

Implementation:

- Added design/README.md and design/project-shape.md
- Defined Go CLI/daemon, @sikong/workspace, and agent-loop boundaries
- Marked old packages/sikong as source material rather than a direct-copy target
- Updated README and AGENTS.md to point at current design and remove stale tooling references

Verification:

- bun run fmt:check
- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Write project-shape docs before importing old Sikong code so the migration has a stable boundary

Next adjustment:

- Design the fixed multi-worker coordination protocol separately from the project shape

## 2026-06-13 - Document PlanDef coordination engine

Target:

- Record the settled high-level design for Sikong's durable multi-worker coordination engine

Implementation:

- Added design/coordination-engine.md covering roles, PlanDef, ordered stages, worker runTask execution, stage review, final review, and event flow
- Updated the design index and project-shape document to make coordination-engine.md the coordination baseline
- Documented exclusions from the first version: arbitrary workflow DSL, dependency graph, worker transitions, guard expressions, and custom Sikong progress tools

Verification:

- bun run fmt:check
- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep Sikong's process model fixed and small: planner creates the ordered PlanDef, workers execute runTask, reviewers judge acceptance, and lead owns plan/final decisions

Next adjustment:

- Start packages/workspace with pure event/reducer tests before adding runtime execution

## 2026-06-13 - Document workspace management and preferences boundary

Target:

- Record the workspace terminology, registry, preferences abstraction, and lead-controlled preference maintenance

Implementation:

- Added design/workspace-management.md with Sikong data dir vs workspace terminology, WorkspaceDef, WorkspaceStore, WorkspacePreferences, and WorkspacePreferencesFactory
- Updated project-shape.md to use workspaces instead of projects and to include data-dir/workspace package boundaries
- Updated coordination-engine.md so workspace preferences are lead-controlled rather than automatically injected into worker runs

Verification:

- bun run fmt:check
- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Treat workspace preferences as lead-maintained YAML entries for stable project preferences and constraints, not as worker progress state or automatic run context

Next adjustment:

- Implement workspace/data-dir primitives before wiring task coordination to runtime execution

## 2026-06-13 - Refine workspace as state namespace

Target:

- Clarify that workspace directories are Sikong state namespaces and not agent execution directories

Implementation:

- Reduced WorkspaceDef to id and name only
- Removed allowedPaths, git, defaultWorkerId, and env from the workspace definition
- Added workspace directory vs agent cwd rules and moved git repo/worktree resolution to runtime/run context
- Updated project-shape.md state layout to place worktrees under workspaces/<workspaceId>/
- Updated coordination-engine.md with the invariant that runtime must provide agent cwd and git work should use workspace-owned worktrees

Verification:

- bun run fmt:check
- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Treat workspace as Sikong's project-level state namespace; execution inputs and cwd belong to runtime/run context

Next adjustment:

- Define runtime-provided cwd and first simple git worktree allocation policy during implementation

## 2026-06-13 - Implement workspace data dir and preferences primitives

Target:

- Add the first @sikong/workspace implementation slice for workspace state namespaces and preferences

Implementation:

- Added packages/workspace with data dir layout helpers, file-backed WorkspaceStore, file-backed WorkspacePreferences, and worktree path helpers
- Kept WorkspaceDef minimal with only id and name
- Stored workspace definitions in workspace.yaml and preferences in preferences.yaml
- Added Bun tests for data dir resolution/layout, workspace CRUD, preferences read/write/append, and worktree path helpers
- Wired root typecheck and check:js scripts to include @sikong/workspace

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test
- bun run fmt
- bun run check
- bun run fmt
- bun run check
- bun run fmt
- bun run check
- bun run fmt
- bun run check
- bun run typecheck
- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Build workspace as a pure state namespace first; keep runtime cwd and git repository discovery outside WorkspaceDef

Next adjustment:

- Implement coordination events/reducer against workspaceId without adding runtime policy to workspace definitions

## 2026-06-14 - Define CLI, Client Agent, and command surface boundaries

Target:

- Clarify the caller-facing shape before wiring workspace handlers or task commands

Implementation:

- Added cli.md for the external-agent-facing CLI contract
- Added client-agent.md for the UI-embedded Client Agent and client work-log boundaries
- Added command-surface.md so CLI and tools share command handlers instead of duplicating logic
- Updated project-shape.md to distinguish External Agent, Client Agent, and internal Task Lead
- Updated workspace-management.md to keep workspace preferences separate from client work-log memory

Verification:

- bun run fmt
- bun run check

Verifier:

- self-check

Drift: closer

Method feedback:

- Treat CLI and UI tools as adapters over shared command handlers; do not let either adapter own Sikong coordination semantics

Next adjustment:

- Implement workspace/preference command handlers first, then attach CLI and typed tool adapters

## 2026-06-14 - Implement coordination event and projection core

Target:

- Add the first execution-engine foundation inside @sikong/workspace without coupling to agent runtime execution

Implementation:

- Added PlanDef, PlanStageDef, TaskEvent, TaskProjection, worker-run projection, stage-review projection, and final-review projection types
- Added a pure task-event reducer for task creation, plan request/submit/accept/reject, stage start, worker results, stage review, final review, and terminal task events
- Added file-backed JSONL task event store and JSON projection store under workspace state paths
- Added task event/projection path helpers and exported coordination APIs from @sikong/workspace
- Added reducer and store tests covering the happy path, rejected plan path, mixed-task event rejection, JSONL append/read, and projection rebuild

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep the coordination core event-sourced and runtime-free; command handlers and agent-loop execution should attach after reducer/store behavior is stable

Next adjustment:

- Implement workspace/preference command handlers, then task create/show/inspect handlers over the coordination store

## 2026-06-14 - Implement shared command handlers

Target:

- Add the shared command surface beneath future CLI and typed tool adapters

Implementation:

- Added CommandResult, CommandError, CommandContext, stable error-code types, and deterministic command id/time hooks for tests
- Added workspace handlers for create/list/get/delete over FileWorkspaceStore
- Added preference handlers for list/add/remove over FileWorkspacePreferences
- Added task handlers for create/get and inspect summary/events/projection/trace over the coordination event and projection stores
- Made task create append task.created plus plan.requested, then rebuild the task projection
- Added runtime path validation for supplied cwd and repoPath without starting agent execution
- Added command handler tests covering workspace, preference, task create/show/inspect, and structured error paths

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep CLI and tool adapters thin; shared command handlers own validation and storage calls but still do not own worker scheduling or runtime execution

Next adjustment:

- Add the Go CLI adapter for workspace, preference, and read-only task inspect commands before adding live worker execution

## 2026-06-14 - Correct daemon and Bun process boundary

Target:

- Prevent the new Go/Bun boundary from recreating the old Bun-singleton concurrency bottleneck

Implementation:

- Added daemon-runtime.md to define CLI one-shot execution separately from daemon-supervised concurrent runtime processes
- Updated project-shape.md to make Go daemon responsible for child-process supervision, cancellation, timeout, and concurrency limits
- Updated cli.md to clarify that one Go CLI invocation may delegate to one Bun command process, but this does not define daemon runtime execution
- Updated command-surface.md to require request-scoped handlers and call out per-task file locking before daemon concurrency

Verification:

- bun run fmt
- bun run check

Verifier:

- self-check

Drift: closer

Method feedback:

- Keep CLI one-shot for external agents, but design sikongd as a Go supervisor over many Bun child processes, not one long-running Bun singleton

Next adjustment:

- Add per-task file append locking before enabling concurrent daemon-managed worker/reviewer writes

## 2026-06-14 - Remove agent-role semantics from daemon design

Target:

- Keep Go daemon as a generic process supervisor instead of an agent orchestrator

Implementation:

- Updated daemon-runtime.md to define ProcessRunSpec and make labels non-semantic inspect metadata
- Clarified that Go daemon must not branch on planner, worker, reviewer, or Task Lead concepts
- Moved Agent Lead orchestration and planner/worker/reviewer semantics back to the TypeScript workspace engine
- Updated project-shape.md, cli.md, and command-surface.md to keep daemon concurrency resource-based rather than role-based

Verification:

- bun run fmt
- bun run check

Verifier:

- self-check

Drift: closer

Method feedback:

- Do not define Agent kind at the daemon/runtime-supervisor boundary; use generic process runs and let TS engine interpret results into domain events

Next adjustment:

- Add per-task file append locking, then define the generic process supervisor API without agent role fields

## 2026-06-14 - Add Bun-side per-task event locking

Target:

- Complete the Bun-side storage hardening needed before daemon-managed parallel child processes write task events

Implementation:

- Added a cross-process file lock helper based on atomic lock-file creation
- Added taskEventsLockFile path helper and exported the file-lock utility
- Wrapped FileTaskEventStore append operations in a per-task lock
- Added appendManyAndRebuildProjection so event append and projection rebuild happen under the same task lock
- Updated task create to use the locked append/rebuild path
- Added a concurrent same-task append test that verifies JSONL and projection completeness after parallel writes
- Updated daemon-runtime.md and command-surface.md to mark Bun-side event append locking as implemented

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Put file-backed concurrency protection inside the Bun store layer so CLI, tools, and future daemon child processes share the same safe write path

Next adjustment:

- Define the generic ProcessRunSpec daemon API and keep it free of agent role/kind fields

## 2026-06-14 - Implement Bun generic process runner

Target:

- Add the Bun-side subprocess execution unit that Go daemon can supervise later

Implementation:

- Added ProcessRunSpec, ProcessRunResult, and ProcessRunStatus types without agent kind or role fields
- Added runProcess(spec) using Bun.spawn to execute one subprocess with cwd, env, stdin, stdout/stderr capture, exit code, duration, and timeout handling
- Added process/runner.ts entrypoint that reads a spec from --spec file or stdin and prints one structured JSON result
- Added npm script `process-runner` for package-local invocation
- Exported the process runner APIs from @sikong/workspace
- Added tests for success, non-zero exit, cwd/env/stdin, timeout, spec-file runner behavior, invalid specs, and entrypoint JSON output
- Updated daemon-runtime.md and project-shape.md to mark the Bun process runner as implemented

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep subprocess execution generic; role-specific planner/worker/reviewer semantics stay in TS engine orchestration, not in ProcessRunSpec

Next adjustment:

- Define the Go daemon local API that starts generic ProcessRunSpec child processes and returns ProcessRunResult

## 2026-06-14 - Lock coordination protocol boundaries

Target:

- Record the already-decided plan confirmation and worker result boundaries in the design so implementation cannot drift

Implementation:

- Clarified that submitted plans are accepted or rejected only by the lead; there is no plan reviewer path in the protocol
- Clarified that planner output must be submitted through `submit_plan`
- Clarified that worker results must come from the terminal `agent-loop.runTask` task tool, or an adapter-equivalent terminal schema
- Clarified that stdout, stderr, exit code, timeout, and duration are process facts only; they must not be interpreted as worker task results

Verification:

- bun run fmt
- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep agent protocol results separate from generic subprocess facts; avoid adding reviewer or parser paths that were already ruled out

Next adjustment:

- Continue with the Go daemon local API that starts generic ProcessRunSpec child processes and returns ProcessRunResult

## 2026-06-14 - Reorganize workspace package source layout

Target:

- Make the Bun workspace package easier to scan by aligning directories and file names with architecture boundaries

Implementation:

- Replaced the `home` terminology with `dataDir` / `data-dir` for the local durable data root
- Moved data-dir layout, YAML helpers, and file locking into `src/data-dir/`
- Moved workspace registry and preference storage into `src/workspace/`
- Moved CLI adapter into `src/cli/`
- Kept coordination, command, and process code under their existing architecture directories
- Renamed command handler files to resource-oriented names: `preference.ts` and `task.ts`
- Added `src/README.md` documenting the package source layout and ownership boundaries
- Updated docs and CLI tests to use `--data-dir` instead of `--home`

Verification:

- bun run fmt
- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Prefer common infrastructure naming such as data dir over a project-specific home concept; keep package directories aligned to ownership boundaries

Next adjustment:

- Define the Go daemon local API that starts generic ProcessRunSpec child processes and returns ProcessRunResult

## 2026-06-14 - Implement task protocol command handlers

Target:

- Implement the accepted task coordination protocol without adding workflow DSL, Agent kind, or stdout/stderr result inference

Implementation:

- Added validated command handlers for plan submission, lead plan accept/reject, worker run start, worker terminal complete/fail/budget-exceeded results, stage review, final review recommendation, and final lead task closure
- Made plan submission assign engine-owned plan/stage ids and versions
- Made plan acceptance start the first ordered stage
- Made accepted stage review advance to the next ordered stage or start final review after the last stage
- Required failed and budget-exceeded worker results to include report or note in addition to summary
- Exposed the same protocol handlers through thin CLI JSON adapters
- Updated design docs to distinguish validated protocol commands from forbidden low-level reducer shortcuts

Verification:

- bun run fmt
- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep terminal task tool semantics at the command boundary; process facts remain separate from worker task results

Next adjustment:

- Connect live worker execution to `agent-loop.runTask` through an adapter that calls these protocol handlers

## 2026-06-14 - Connect runTask worker adapter

Target:

- Connect one live worker run to `agent-loop.runTask` without adding backend selection, daemon orchestration, or process-output result inference

Implementation:

- Added `packages/workspace/src/runtime/worker.ts` as a thin adapter over command handlers
- Made `runTaskWorker` start a worker run, call an injected `runTask` function, and record completed, failed, or budget-exceeded results through terminal worker command handlers
- Added worker goal construction from task request, current stage objective, and stage acceptance criteria
- Recorded thrown runtime failures as failed worker runs with an explicit report
- Added `agent-loop` as a workspace dependency for `@sikong/workspace`
- Added runtime adapter tests for completed, failed, and budget-exceeded `runTask` results

Verification:

- bun install
- bun run fmt
- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep backend/runtime choice outside workspace coordination; inject `runTask` and let the adapter only translate terminal task results into coordination events

Next adjustment:

- Add daemon/API wiring that can start generic child processes without importing task role semantics into Go

## 2026-06-14 - Refactor runtime to worker-run presets

Target:

- Implement the worker-unified runtime model while keeping planner, executor, and verifier as preset wrappers instead of code-level agent roles

Implementation:

- Replaced `runtime/worker.ts` with `runtime/worker-run.ts` as the generic worker-run core
- Kept `runTaskWorker` as a compatibility export while making `runWorkerTask` the primary worker task bridge
- Added planner, executor, and verifier preset wrappers that assemble prompt, tools, skills, and context without writing durable state
- Added a pure orchestration tick that maps task projection state to the next preset action or lead decision wait point
- Added tests for preset tool composition, review-only prompts, orchestration decision points, and the worker-run task bridge
- Updated design docs and source layout documentation to state that planner/verifier files are preset wrappers, not agent role implementations

Verification:

- bun run fmt
- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep reusable planner/verifier files as preset composition helpers; durable state changes still go through command handlers and terminal tools

Next adjustment:

- Wire the pure orchestration action to a process/daemon execution path without adding agent roles to Go or ProcessRunSpec

## 2026-06-14 - Record phased implementation plan

Target:

- Preserve the agreed next-phase plan in design docs so it is not only stored in chat context

Implementation:

- Added `design/implementation-plan.md` with the current baseline, phase order, module deliverables, and fixed boundaries
- Updated `design/README.md` so the plan is part of the design read order

Verification:

- bun run fmt:check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep long-running implementation direction in design docs, not only in conversation history

Next adjustment:

- Start Phase 1: Go generic process supervisor

## 2026-06-14 - Implement Go generic process supervisor

Target:

- Complete Phase 1 of the implementation plan without adding agent orchestration semantics to Go

Implementation:

- Added generic `ProcessRunSpec`, `ProcessRunResult`, and `ProcessRunStatus` types under `internal/daemon`
- Added `ProcessRunner` with cwd, env, stdin, timeout, cancellation, stdout/stderr capture, exit code, signal, duration, and labels as process facts
- Added `ProcessSupervisor` to retain completed process run results by run id
- Added Go tests for success, non-zero exit, timeout, cancellation, parallel runs, stored results, and absence of role/kind fields in the process spec JSON
- Updated daemon implementation docs to mark the generic process runner/supervisor as implemented while leaving local daemon API as the next phase

Verification:

- gofmt -w internal/daemon/process.go internal/daemon/process_test.go
- go test ./internal/daemon

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Build the daemon bottom-up as generic process facts first; do not let local API or orchestration concerns leak into the process runner

Next adjustment:

- Start Phase 2: daemon local API and TS process client

## 2026-06-14 - Implement daemon process API and TS client

Target:

- Complete Phase 2 by exposing the generic Go process supervisor through a
  local API and adding a TypeScript client without adding agent/task semantics
  to the daemon

Implementation:

- Added asynchronous `ProcessSupervisor` lifecycle methods for start, get,
  wait, and cancel over generic process snapshots
- Added a process-only HTTP API under `internal/daemon` with health,
  process-run start/get/wait/cancel endpoints
- Wired `sikongd` to serve the local API on `127.0.0.1:8765` by default, with
  `SIKONG_DAEMON_ADDR` as the override
- Added `DaemonProcessClient` in `packages/workspace/src/process/client.ts`
  and exported process snapshot/state types
- Added Go and Bun tests for async lifecycle, API lifecycle, cancellation, and
  client request/error handling
- Updated daemon runtime and implementation-plan docs to mark Phase 2 as
  implemented while leaving orchestration action execution as Phase 3

Verification:

- gofmt -w internal/daemon/daemon.go internal/daemon/daemon_test.go internal/daemon/process.go internal/daemon/process_api.go internal/daemon/process_test.go
- go test ./internal/daemon
- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep the daemon API process-only; TypeScript can request and interpret
  process facts without moving task state semantics into Go

Next adjustment:

- Start Phase 3: connect orchestration actions to daemon-managed process runs

## 2026-06-14 - Add orchestration action executor

Target:

- Start Phase 3 by connecting pure orchestration actions to existing runtime
  and command-handler primitives without creating code-level agent roles

Implementation:

- Added `executeOrchestrationAction` under `packages/workspace/src/orchestration`
- Loop-backed planning and verification actions call `runWorkerLoop` with
  injected loop runtime
- Stage worker actions call `runWorkerTask` with injected `runTask`
- Stage review start actions call the existing `startStageReview` command
  handler
- Lead decision, terminal, and blocked actions return structured wait/terminal
  states and do not mutate task state
- Added orchestration executor tests for loop-backed actions, lead wait state,
  stage worker completion, and review start
- Updated implementation docs to mark the executor as implemented while keeping
  daemon-backed subprocess action entrypoints as pending

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep orchestration execution as action dispatch over existing primitives; the
  daemon-backed step needs a separate serialization boundary for presets/tools

Next adjustment:

- Design and implement daemon-backed subprocess entrypoints for orchestration
  actions without serializing live tool functions through JSON

## 2026-06-14 - Add orchestration subprocess runner

Target:

- Complete the Phase 3 process boundary by allowing orchestration actions to run
  inside daemon-managed Bun child processes without serializing live tool
  functions

Implementation:

- Added `src/orchestration/runner.ts`, a Bun subprocess entrypoint that reads a
  JSON request, loads an optional runtime module, and calls
  `executeOrchestrationAction`
- Added serializable runner request/action types that keep live loop, tool, and
  hook functions out of JSON
- Added `createOrchestrationProcessSpec` and `startOrchestrationProcess` so the
  TypeScript orchestration layer can wrap runner requests as generic daemon
  process specs
- Added an end-to-end subprocess test that writes a runtime module, starts the
  runner through `runProcess`, and verifies the worker terminal event is
  recorded in task state
- Updated daemon runtime and implementation-plan docs to mark the subprocess
  boundary as implemented and leave named tool/preset registries as the next
  production assembly step

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Use runtime modules as the subprocess assembly boundary; JSON carries data,
  while live tools and runtime permissions are created inside the child process

Next adjustment:

- Add compact inspect view for next action and wait state, then decide whether
  named tool/preset registries are needed before production worker execution

## 2026-06-14 - Add compact task inspect view

Target:

- Complete Phase 4 by giving external agents and lead-facing surfaces a compact
  task view with current status, next action, and lead wait state

Implementation:

- Added `inspectTaskCompact` command handler
- Added `TaskCompactView` and data-only next-action summaries without exposing
  loop, tool, or preset functions
- Added CLI support for `inspect compact <taskId>`
- Added command and CLI tests for planning, submitted-plan wait state, and
  post-worker stage-review next action
- Updated the implementation plan to mark the compact inspect view as
  implemented

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep inspect views derived and data-only; do not expose executable preset
  objects or function-bearing action payloads through the CLI

Next adjustment:

- Decide whether named tool/preset registries are required before enabling
  production runtime-module execution

## 2026-06-14 - Tighten orchestration runner and inspect boundaries

Target:

- Correct the drift found during design review: runner request JSON must be
  data-only, and compact inspect must not duplicate orchestration planning
  rules in the command layer

Implementation:

- Added `toSerializableOrchestrationAction` and data-only serializable action
  types for orchestration runner requests
- Stripped executable fields from runner JSON: tools, skills, MCP servers,
  hooks, loop factories, and `runTask`
- Added optional `hydrateOrchestrationAction` on runtime modules so live tools
  and executable runtime context can be assembled inside the child process
- Moved compact next-action summaries into `orchestration/summary.ts`
- Updated `inspectTaskCompact` to reuse the orchestration-owned summary helper
  instead of carrying a second copy of transition rules
- Added tests that verify serialized runner requests do not contain executable
  field keys

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep one owner for orchestration next-action rules; command surfaces should
  render data-only summaries, not reimplement planning logic

Next adjustment:

- Re-run full check and static role/kind search before the next implementation
  slice

## 2026-06-14 - Add runtime assembly registry

Target:

- Add the named runtime/tool-profile assembly boundary needed by
  daemon-managed orchestration subprocesses without introducing agent role or
  kind semantics

Implementation:

- Added `RuntimeAssemblyRegistry` for named backend factories and named
  tool-profile factories
- Added `createRuntimeAssemblyModule` so `orchestration/runner.ts` can hydrate
  serializable actions with live tools inside the child process
- Added data-only `runtimeAssembly` support on orchestration runner requests so
  subprocesses can use the default registry without a separate runtime module
  file
- Added built-in mock backend and empty tool profile as the default registry
  surface
- Added default agent-loop backend names for `ai-sdk`, `claude-code`, `codex`,
  and `cursor`
- Added Sikong protocol profiles for `submit_plan`, stage review decisions, and
  final review recommendation tools over command handlers
- Hydrated planning, execution, stage verification, and final verification
  actions by composition of named tool profiles
- Added tests proving runtime module hydration and direct `runtimeAssembly`
  loading work while runner request JSON remains data-only and free of
  role/kind fields, and protocol tools write durable task decisions
- Updated implementation and daemon runtime docs to mark the assembly boundary
  as implemented

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Treat backend/profile names as runtime assembly configuration only; do not
  use them as planner/executor/verifier agent kinds

Next adjustment:

- Register production inspection/execution tool profiles and backend permission
  defaults, then start Phase 6 parallel stage worker policy when the serial
  path is fully exercised

## 2026-06-14 - Add initial parallel stage worker policy

Target:

- Let a single ordered stage request multiple worker runs without adding
  dependency graphs, agent roles, or worker-driven transitions

Implementation:

- Added optional `PlanStageDef.workerCount`, defaulting to serial execution
  when omitted or `1`
- Accepted `workerCount` through command handlers, CLI plan JSON, and Sikong's
  `submit_plan` protocol tool
- Added `await_worker_results` as a pure orchestration wait action when the
  target number of workers has been started but not all have terminal results
- Updated compact next-action summaries to expose the worker-results wait state
- Added orchestration tests for starting worker runs up to `workerCount`,
  waiting while runs are active, and starting stage review only after all target
  runs are terminal

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep stage parallelism as a per-stage count and scheduler behavior; do not
  add worker roles, dependency graphs, or worker transition APIs

Next adjustment:

- Register production inspection/execution tool profiles and backend permission
  defaults

## 2026-06-14 - Client Agent UI and Vite config alignment

Target:

- Implement the first Client Agent UI slice while preserving the Crabbro-style
  boundary that transcript is presentation state, not agent memory

Implementation:

- Added client work-log, context-packet, and per-turn client-agent facade under
  `packages/workspace/src/client-agent`
- Extended the client-agent tool adapter with task card listing and wait-boundary
  support
- Added `packages/client` as a React/Vite/shadcn-compatible UI with a single
  activity stream, workspace switching, task cards, and secondary detail views
- Split the client TypeScript config into Vite-style app/node project configs
  and moved the Bun API server outside browser `src`

Verification:

- bun run check
- bunx --bun shadcn@latest info --json
- HTTP smoke check for `/api/health`, `/api/state`, and Vite root page

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- For Vite/shadcn packages, start from the current official Vite split-tsconfig
  shape and then adapt only the parts required by TS Native Preview; do not make
  root tsconfig exclusions the first fix

Next adjustment:

- Replace the dev-default mock Client Agent loop with explicit runtime selection
  once the client-agent runtime policy is designed

## 2026-06-14 - Add client-agent tool adapter

Target:

- Expose the implemented command surface to a UI/client-agent without routing
  through CLI argv parsing

Implementation:

- Added `createClientAgentTools` under `packages/workspace/src/tools`
- The adapter wraps workspace, preference, task creation, and task inspect
  command handlers as `agent-loop` tools
- Included the compact inspect view so client task cards can render next action
  and runtime process state without reading full projections
- Added focused tests for successful tool calls and structured invalid-input
  errors
- Updated design docs to mark the tool adapter implemented while keeping
  steer/monitor/client runtime deferred

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test
- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep client-agent tools as a thin command-handler adapter; do not mix in
  task protocol tools or code-level agent roles

Next adjustment:

- Continue with non-steer missing pieces after checking the design backlog

## 2026-06-14 - Add daemon start and stop commands

Target:

- Complete daemon process-control commands without moving task orchestration
  semantics into the Go daemon or CLI adapter

Implementation:

- Added `POST /shutdown` to the daemon local API
- Wired shutdown through the daemon run context so it uses the existing graceful
  HTTP server shutdown path
- Added `DaemonProcessClient.shutdown`
- Added `sikong daemon start`, which checks health, starts `sikongd` when
  needed, and waits for health
- Added `sikong daemon stop`, which calls the shutdown endpoint
- Kept daemon start/stop separate from task projections, process-run results,
  and task terminal decisions
- Added Go API tests and TypeScript CLI/client tests

Verification:

- go test ./...
- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test
- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Prefer a daemon-owned shutdown endpoint over pid-file-only stop behavior so
  process control works for already-running daemons too

Next adjustment:

- Leave steer deferred until the input channel for running loops is designed

## 2026-06-14 - Add daemon status CLI adapter

Target:

- Expose the existing daemon health endpoint to external agents as a simple CLI
  command without moving coordination semantics into the daemon

Implementation:

- Added `sikong daemon status [--daemon <url>]`
- The command calls `/health` through `DaemonProcessClient`
- Successful output includes the normalized daemon base URL and health payload
- Client failures return structured `daemon_error`

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep daemon commands as process-control adapters; task progress still flows
  through command handlers and the TypeScript orchestration driver

Next adjustment:

- Re-run full repository check and continue only with gaps that are already in
  the design

## 2026-06-14 - Add task-level git worktree allocation

Target:

- Ensure git-backed tasks run in a workspace-owned worktree instead of using the
  source repository as the agent cwd

Implementation:

- Added `allocateTaskWorktree` under the workspace package
- `task create --repo` now resolves the git repository root, creates a detached
  worktree at `workspaces/<workspaceId>/worktrees/<taskId>/`, and stores that
  path as `runtime.cwd`
- `--cwd` remains the non-git direct cwd path, and `--cwd` plus `--repo` is
  rejected as ambiguous input
- Added a command-handler test using a real temporary git repository and
  verifying the task runtime cwd points at the workspace-owned worktree

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep git repository paths as runtime input; materialize agent cwd under the
  workspace without expanding `WorkspaceDef`

Next adjustment:

- Continue with process-control gaps only after worktree isolation is verified
  by the full repository gate

## 2026-06-14 - Add runtime process facts and cancel

Target:

- Make runtime process cancellation possible without adding a scheduler or
  treating process output as task results

Implementation:

- Added `runtime_process.started` and `runtime_process.finished` task events
- Task projections now keep runtime process facts keyed by daemon process id
- The process-backed orchestration executor records start and finish facts
  around daemon-managed process execution
- Added `sikong task cancel <taskId> [--daemon <url>]`, which cancels currently
  running daemon process ids recorded on the task
- Cancellation records process facts only; it does not mark the durable task as
  rejected or completed

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep process cancellation separate from task terminal decisions; process facts
  are not worker task results

Next adjustment:

- Leave steer deferred until there is an explicit input channel design for
  running agent loops

## 2026-06-14 - Expose runtime process facts in inspect views

Target:

- Make recorded runtime process facts visible through normal inspect surfaces
  so external agents can decide whether `task cancel` is useful

Implementation:

- `inspectTaskSummary` now reports total and running runtime process counts
- `inspectTaskCompact` now includes runtime process totals and the latest
  runtime process fact
- Trace already includes the new runtime process events, and tests now assert
  summary, compact, and trace visibility

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Process cancellation needs visible process facts; expose them through inspect
  instead of adding a separate scheduler or monitor layer

Next adjustment:

- Prefer small command/inspect closure work until steer has a concrete input
  channel design

## 2026-06-14 - Add read-only task wait command

Target:

- Give external agents a simple way to wait for a durable task to reach the
  next observable boundary without adding scheduler state

Implementation:

- Added `waitTask` in the task command layer
- The wait boundary reuses compact task semantics: lead decision required,
  worker results pending, terminal, or blocked
- Added `sikong task wait <taskId> --workspace <workspaceId> [--timeout-ms <n>]`
  in the CLI adapter
- Added command and CLI tests for an already submitted plan returning
  immediately at `await_plan_decision`

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep waiting read-only; continuing task progress still belongs to
  projection-driven `task drive`

Next adjustment:

- Continue closing implemented command/runtime gaps without introducing a
  long-lived Bun scheduler

## 2026-06-14 - Add production runtime assembly defaults

Target:

- Close the runtime assembly gap for task cwd, adapter permissions, and
  inspection/execution tool profiles without creating a backend-agnostic Sikong
  tool layer

Implementation:

- Resolved task runtime cwd from the orchestration runner request and durable
  task projection
- Applied backend defaults inside runtime assembly:
  `claude-code` gets `cwd` and `allowedPaths`, `codex` gets `cwd`,
  `fullAuto: true`, and `workspace-write` sandbox, and `cursor` gets `cwd` plus
  enabled local sandbox unless caller options override them
- Added `ai-sdk-local-inspection` and `ai-sdk-local-execution` tool profiles
  using `agent-loop`'s AI SDK local tool bundle
- Kept AI SDK inspection read/search/web-only while execution receives the full
  local tool bundle
- Added tests proving tool profiles resolve task runtime cwd and do not expose
  write/bash tools through inspection

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Treat AI SDK local tools as an explicit profile and keep Claude/Codex/Cursor
  permission behavior in adapter-native options

Next adjustment:

- Add a CLI/command entrypoint that invokes the orchestration driver against
  daemon-managed process execution

## 2026-06-14 - Add CLI task drive entrypoint

Target:

- Expose the orchestration driver to external agents through the CLI without
  moving scheduling semantics into the CLI adapter

Implementation:

- Added `sikong task drive <taskId>` to the TypeScript CLI
- The command builds default runtime assembly names, creates a daemon process
  client, and calls `runOrchestrationUntilWait`
- Runtime-backed actions run through `executeOrchestrationActionProcess`; pure
  wait/terminal/review-start actions use the local executor
- Added CLI test coverage with a fake daemon process client that mutates durable
  task state the same way a runner process would
- Updated CLI, command surface, daemon runtime, and implementation-plan docs

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test -- src/cli/cli.test.ts

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep CLI as an adapter over orchestration APIs; classify runtime-backed
  actions before sending work to daemon processes

Next adjustment:

- Add runtime process steer/cancel command surface

## 2026-06-14 - Add process-backed orchestration action executor

Target:

- Bridge one orchestration action to a daemon-supervised runner process without
  moving orchestration semantics into the Go daemon

Implementation:

- Added `executeOrchestrationActionProcess` in the orchestration process module
- The executor writes one data-only runner request, creates a generic
  `ProcessRunSpec`, starts and waits through a daemon process client, and parses
  `OrchestrationRunnerOutput` from stdout
- Process failure, timeout, missing result, or invalid JSON become structured
  command errors instead of task result inference
- Added tests with a fake daemon process client proving the request JSON stays
  free of live tools and role/kind fields

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Treat runner stdout as a transport envelope only; task protocol results must
  still be written by tools/command handlers inside the runner

Next adjustment:

- Add a CLI/command entrypoint that invokes the orchestration driver against
  daemon-managed process execution

## 2026-06-14 - Add orchestration driver loop

Target:

- Add the TypeScript execution loop that keeps a task moving across non-lead
  orchestration actions until it reaches a wait, terminal, blocked, or action
  budget boundary

Implementation:

- Added `runOrchestrationUntilWait` under `packages/workspace/src/orchestration`
- The driver reloads durable task projection before each action, calls
  `planNextOrchestrationAction`, executes the action, and reloads projection
  after mutations
- Added stop reasons for lead wait, worker-results wait, terminal, blocked,
  and `maxActions`
- Kept execution pluggable so callers can use the default in-process executor
  or a daemon/subprocess executor without changing the driver semantics
- Added tests for planning-to-plan-decision and running-stage-to-final-decision
  flows
- Updated design docs to keep the driver in TypeScript and out of the Go daemon

Verification:

- bun --filter @sikong/workspace typecheck
- bun --filter @sikong/workspace test

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Model sustained task progress as a stateless projection-driven loop; avoid
  long-lived scheduler state unless a later requirement proves it is needed

Next adjustment:

- Register production inspection/execution tool profiles and backend permission
  defaults

## 2026-06-14 - Implement typed client message rendering

Target:

- Implement Sikong client message parts and restricted UI renderer from design/client-agent.md

Implementation:

- packages/client/src/types.ts, messages.ts, message-renderer.tsx, App.tsx, server/index.ts

Verification:

- bun run check; /api/turn smoke on port 8786 returned message.parts=[text]; desktop/mobile screenshots under /tmp/sikong-message-protocol-\*.png

Verifier:

- self-check with deterministic command evidence and screenshots

Drift: closer

Method feedback:

- Keep dynamic UI as a restricted client renderer catalog, not a command-capable UI DSL

Next adjustment:

- Wire real client-agent backend output to produce ui parts when needed

## 2026-06-15 - Preserve unified config during settings writes

Target:

- Keep Sikong runtime settings in the single config.yaml without losing other config fields

Implementation:

- FileSettingsStore now merges normalized version/defaults into existing config.yaml; client API adds CORS for cross-port dev UI; tests cover defaultWorkerId preservation

Verification:

- bun run check; headless Chrome CDP smoke loaded Settings, saved, showed Saved, and config retained defaultWorkerId plus deepseek provider

Verifier:

- self-check with deterministic tests and headless browser smoke; Browser plugin had no available iab instance

Drift: closer

Method feedback:

- When consolidating config files, treat feature-specific stores as partial owners of the document and smoke-test the real UI transport, not only the store API

Next adjustment:

- Continue with a real Client Agent turn after provider-backed runtime is confirmed under the UI

## 2026-06-15 - Sikong UI worker timeout e2e

Target:

- UI-driven Sikong blog task should start real workers and avoid stuck task projections

Implementation:

- Raised UI/client drive timeout to 10 minutes; process timeout now marks active stage worker runs failed; process runner terminates detached process groups

Verification:

- bun --filter @sikong/workspace typecheck; focused workspace tests; bun run check; real UI/CDP e2e created task_0570e83d and wrote packages/blog files before timeout

Verifier:

- self-check with deterministic tests and live e2e evidence

Drift: closer

Method feedback:

- Real UI e2e must inspect both runtime_process and worker_run projections; process timeout without worker terminal event is a durable stuck-state bug

Next adjustment:

- Retest a fresh blog task after API restart with 10 minute timeout if deeper end-to-end completion is needed

## 2026-06-15 - Add console UI generation spec

Target:

- Persist the compact operations-console visual generation rules in the project design docs

Implementation:

- Added `design/console-ui-generation.md` and linked it from `design/README.md`

Verification:

- `rg "Goat|goat" design packages README.md` returned no matches

Verifier:

- self-check with deterministic search evidence

Drift: closer

Method feedback:

- Keep UI generation rules in design docs so visual polish work has a durable token, density, and status-semantics reference

Next adjustment:

- Apply the spec to future client UI polish passes

## 2026-06-15 - Apply console UI generation spec to client

Target:

- Align the Sikong client with the compact operations-console design spec

Implementation:

- Rebased client theme tokens on semantic dark/light console variables while preserving Tailwind aliases; tightened base typography, focus, scrollbar, and utility styles
- Updated Button, Badge, Input, Textarea, and Card primitives to compact metrics and semantic states; polished shell, composer, context rail, settings modal, task cards, timeline, and dynamic message rendering

Verification:

- bun run fmt; bun --filter @sikong/client typecheck; bun --filter @sikong/client build; bun run check
- Render QA with system Chrome Playwright covered desktop home, settings dialog, mobile home, and mobile context sheet; Browser plugin reported no available iab instance

Verifier:

- self-check with deterministic commands and rendered screenshots

Drift: closer

Method feedback:

- Map existing shadcn/Tailwind aliases to semantic console tokens first, then tighten primitives and composed surfaces; this keeps the visual system coherent without broad component rewrites

Next adjustment:

- Continue applying the same status semantics to richer task tables and timeline states as those UI surfaces expand

## 2026-06-15 - Beta release install and start surface

Target:

- Prepare Sikong beta distribution through sikong.dev and a self-contained macOS arm64 CLI

Implementation:

- Added top-level `sikong start`, `sikong stop`, and `sikong status`; start launches daemon plus UI by default and records local process state
- Added embedded runtime extraction for release builds; compiled workspace, client API, orchestration runner, process runner, daemon, and client dist are bundled into the release CLI
- Added macOS arm64 release packaging, Vercel static install site, install script, and tag-triggered GitHub Release workflow

Verification:

- bun run release:darwin-arm64 v0.0.0-beta.local; release binary smoke for version/start/health/status/stop on ports 18765 and 18776; local install script smoke using file:// release assets; bun run check

Verifier:

- self-check with deterministic commands and live local release smoke; GitHub/Vercel deployment itself remains unrun

Drift: closer

Method feedback:

- Keep source-mode and release-mode runtime resolution explicit: source mode may use Bun workspace paths, but release mode must inject helper paths and static dist paths through environment variables

Next adjustment:

- Tag a beta release and connect the Vercel project/domain to deploy `deploy/site`

## 2026-06-15 - Add client turn progress card

Target:

- Show hierarchical pending progress for Sikong client-agent turns without dumping execution logs into chat

Implementation:

- packages/client/src/App.tsx, messages.ts, types.ts, message-renderer.tsx, turn-progress.ts

Verification:

- bun --filter @sikong/client typecheck; bun --filter @sikong/client build; static render smoke; curl API/Vite health; bun run check

Verifier:

- self-check with deterministic command evidence; Browser iab unavailable, no visual screenshot

Drift: closer

Method feedback:

- When /api/turn is synchronous, use a clearly labeled coarse pending projection first and reserve real event detail for future streaming/SSE.

Next adjustment:

- Consider server-sent turn progress events when the backend exposes durable stage events.

## 2026-06-15 - Add SSE turn stream

Target:

- Support server-sent progress for client-agent turns and document durable work-log boundaries

Implementation:

- packages/client/server/index.ts, packages/client/src/api.ts, App.tsx, types.ts, turn-progress.ts, design/client-agent.md, design/implementation-plan.md

Verification:

- SSE error-stream smoke with temporary SIKONG_DATA_DIR; bun --filter @sikong/client typecheck; bun --filter @sikong/client build; bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep turn progress as transient stream events; only durable semantic decisions, preferences, task summaries, and project status belong in client work log.

Next adjustment:

- Add steer segment events when checkpoint-level runtime steer is available.

## 2026-06-15 - Define client timeline write policy

Target:

- Make client work-log context timeline-based and specify what writes, who writes, and how writes are accepted

Implementation:

- design/client-agent.md

Verification:

- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- For memory-like context, define writer authority and append-only timeline semantics before implementing schema migrations.

Next adjustment:

- Implement ClientTimelineEntry storage and context packet migration from workLog to timeline.

## 2026-06-15 - Stabilize timeline candidate generation

Target:

- Define simple stable triggers and templates for timeline writes, especially summarized user_intent entries

Implementation:

- design/client-agent.md

Verification:

- bun run fmt:check; bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Timeline memory should be driven by typed candidates and deterministic summary templates, with inferred writes requiring UI confirmation.

Next adjustment:

- Implement candidate generation and explicit save/confirm flow before migrating storage.

## 2026-06-15 - Make client timeline event-first

Target:

- Revise client timeline design to avoid intelligent summaries and record factual observed events with evidence

Implementation:

- design/client-agent.md

Verification:

- bun run fmt:check; bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- For durable context, prefer observed event payloads plus evidence references over prose summaries or inferred memory candidates.

Next adjustment:

- Implement timeline mapper from observed events before introducing summarization or compaction.

## 2026-06-15 - Simplify client-agent context model

Target:

- Replace client timeline/work-log context with source stores, bootstrap context, and tool-driven lookup

Implementation:

- design/client-agent.md, design/implementation-plan.md

Verification:

- bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- For multi-project client-agent context, keep transcript/workspace/task stores as source-of-truth and pass only a small bootstrap packet; let tools retrieve detailed history.

Next adjustment:

- Migrate current client-agent context implementation from workLog packet to bootstrap context and transcript/workspace/task inspection tools.

## 2026-06-15 - Implement client-agent bootstrap source context

Target:

- Make client-agent turns start from a small bootstrap packet while keeping transcript, workspace, and task history as tool-queryable source stores

Implementation:

- packages/workspace/src/client-agent/context.ts, packages/workspace/src/client-agent/turn.ts, packages/workspace/src/tools/client-agent-tools.ts, packages/client/server/index.ts, packages/client/src/types.ts

Verification:

- bun run fmt; bun --filter @sikong/workspace test; bun --filter @sikong/client typecheck; bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Keep the bootstrap context minimal and typed, and expose source-store reads as tools so multi-project history does not become a prompt replay problem.

Next adjustment:

- Wire steer/control segment events into the transcript and SSE progress model without adding a second memory layer.

## 2026-06-15 - Fix client turn SSE idle timeout

Target:

- Make long-running client-agent SSE turns survive Bun's request idle timeout and avoid crashing the UI API when a stream closes early

Implementation:

- packages/client/server/index.ts, packages/client/src/App.tsx

Verification:

- Official Bun docs check for `Bun.serve` idleTimeout and per-request `server.timeout(req, 0)`; real headed UI multi-project turn exceeded 10 seconds and completed; second focused macOS workspace turn completed; bun run check

Verifier:

- self-check with deterministic command evidence plus real UI smoke

Drift: closer

Method feedback:

- For SSE on Bun, disable idle timeout per streaming request and make stream enqueue/close idempotent; global timeout increases alone do not protect client-disconnect paths.

Next adjustment:

- Replace remaining legacy work-log labels in inspector surfaces when the source-store context migration is completed.

## 2026-06-15 - Multi-project E2E task execution smoke

Target:

- Exercise real UI conversation, multi-workspace task creation, and parallel task execution through Sikong runtime paths

Implementation:

- No product code changes; test artifacts created under /tmp/sikong-e2e-parallel

Verification:

- UI conversation created Blog E2E task_5a0eaa78-22ab-4628-bda3-e7f7c562b913 and macOS Screenshot App task_7c95cadb-6643-448b-890d-b2a6beb2ed37; parallel /api/tasks/drive ran both planning workers; parallel plan acceptance drove both through stage worker, stage review, stage verification, and final verification; verified /tmp/sikong-e2e-parallel/blog/E2E_BLOG_PARALLEL.txt and /tmp/sikong-e2e-parallel/macos/E2E_MACOS_PARALLEL.txt

Verifier:

- self-check with real UI smoke, API projection reads, file content checks, and service health checks

Drift: design-question

Method feedback:

- Multi-project runtime execution works once tasks are created, but the Client Agent turn can remain open after creating tasks and fail to produce an assistant transcript response; long-running model/tool turns need timeout/cancel/finalization handling separate from SSE transport stability.

Next adjustment:

- Add a client-agent turn timeout/cancellation path and persist a terminal assistant/system message when a model/tool loop is killed or exceeds a turn budget.

## 2026-06-15 - Add client-agent terminal outcome settlement

Target:

- Make client-agent turns end through an explicit structured outcome, and avoid treating a missing terminal tool call as an immediate failure

Implementation:

- packages/workspace/src/client-agent/outcome.ts, packages/workspace/src/client-agent/turn.ts, packages/workspace/src/tools/client-agent-tools.ts, packages/client/server/index.ts, packages/client/src/types.ts

Verification:

- bun run fmt; bun --filter @sikong/workspace test; bun run typecheck; bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Use a small terminal contract (`finishClientTurn`) for report/question/request outcomes; if the work pass ends without it, run a read-only settlement pass that can inspect source stores and submit the missing outcome.

Next adjustment:

- Surface structured outcome cards in the UI instead of flattening every outcome to a markdown text assistant message.

## 2026-06-15 - Remove direct task-control web APIs

Target:

- Prevent the web UI and ad hoc HTTP clients from directly driving tasks or accepting/rejecting plans outside the Client Agent flow

Implementation:

- packages/client/server/index.ts, packages/client/src/api.ts, packages/client/src/App.tsx

Verification:

- bun run fmt; bun run check; restarted Sikong with `bun run dev:cli -- stop && bun run dev:cli -- start --no-open`; verified `POST /api/tasks/drive` and `POST /api/tasks/plan-decision` return 404

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- The web UI may display task state, but direct task progression and lead decisions must not be exposed as generic UI/API controls; those actions need to stay inside the agent/user interaction boundary.

Next adjustment:

- Replace any remaining task-control affordances with message-based Client Agent requests and explicit user-visible pending/handled states.

## 2026-06-15 - Implement single Lead and work-unit execution model

Target:

- Replace the old direct planning/worker-count task flow with one engine-driven team model: Client Agent creates work items, Task Lead submits requirement specs and decisions, planner submits stage plans, Lead plans stage rounds, and workers execute round work units.

Implementation:

- packages/workspace/src/coordination, packages/workspace/src/commands, packages/workspace/src/orchestration, packages/workspace/src/runtime, packages/workspace/src/cli, and related design docs/tests

Verification:

- bun run typecheck; bun --filter @sikong/workspace test; bun run check

Verifier:

- self-check with deterministic command evidence

Drift: closer

Method feedback:

- Remove obsolete protocol action types when replacing the workflow, even if they are no longer reachable; dead compatibility branches make it harder to verify there is only one execution mechanism.

Next adjustment:

- Exercise the new Lead/planner/round/work-unit loop through the UI with a real multi-project task once the runtime model is stable enough for live dogfooding.

## 2026-06-15 - Adapt UI to Lead round work-item model

Target:

- Make the client UI reflect the new Lead/spec/round/work-unit task model and avoid presenting client notes as a task execution work log.

Implementation:

- packages/client/src/App.tsx, packages/client/src/types.ts, packages/client/src/message-renderer.tsx, packages/client/src/turn-progress.ts, packages/workspace/src/commands/task.ts

Verification:

- bun run check; Chrome headless smoke on `http://127.0.0.1:5175/` with latest API on `8777`

Verifier:

- self-check with deterministic command evidence and rendered UI smoke

Drift: closer

Method feedback:

- UI smoke against an already-running API can show stale action labels; start a fresh API process on a known port when validating protocol/UI changes. Also normalize old projection-file shapes at read boundaries so historical workspaces do not break the UI after adding projection fields.

Next adjustment:

- Add a first-class task event timeline panel for Work Items instead of relying on optional saved notes or transcript text to explain execution progress.

## 2026-06-16 - Center task detail UI

Target:

- Expose workspace task plan, stage, round, worker, thinking, and tool-call details in the center UI

Implementation:

- Added task detail API/view, worker observations, center work detail, mobile Work sheet

Verification:

- bun run check; Playwright fallback desktop and mobile smoke with system Chrome

Verifier:

- self-check with deterministic tests and browser smoke

Drift: closer

Method feedback:

- Mobile workspace switching should preserve the Work sheet open state for browsing across projects.

Next adjustment:

- Consider URL-addressable task detail routes if deep linking becomes necessary.

## 2026-06-16 - Prepare beta.2 agent role release

Target:

- Clarify Client Agent, Task Lead, Planner, Worker, and Reviewer prompt responsibilities, then publish the next beta installer target.

Implementation:

- Updated client-agent and orchestration prompts, runtime presets, final lead protocol tool wiring, and installer default version.

Verification:

- bun run check; bun run release:darwin-arm64 v0.2.0-beta.2

Verifier:

- self-check with deterministic command evidence and local release build

Drift: closer

Method feedback:

- Prompt boundaries work better as role responsibilities and handoff ownership than as lists of prohibited actions.

Next adjustment:

- Dogfood the released build with several real workspace tasks before treating the beta as stable daily infrastructure.

## 2026-06-16 - Terminal tool stop boundary

Target:

- Route terminal tool completion through backend-safe stop boundaries

Implementation:

- Claude requestStop -> PostToolBatch continue:false; Codex requestStop -> post-tool-result turn/interrupt normalization; Cursor/AI SDK already use step boundaries

Verification:

- bun run check passed

Verifier:

- self-check with deterministic test/build evidence; real Claude/Codex external smoke not run

Drift: closer

Method feedback:

- Terminal protocol tools should set stop intent; adapters own when to stop at native safe boundaries

Next adjustment:

- Run real Claude/Codex smoke before release if credentials/runtime are available

## 2026-06-16 - Client API service robustness

Target:

- Make Sikong UI/client-api survive long turns and expose actionable diagnostics for remote machines.

Implementation:

- Added sikong logs, disabled Bun idle timeout, added client-api error hooks/health uptime/status log paths, and published v0.2.0-beta.9.

Verification:

- bun run check; live logs/status/health smoke; GitHub release v0.2.0-beta.9 and sikong.dev installer verified.

Verifier:

- self-check with CI release evidence

Drift: closer

Method feedback:

- For beta service crashes, ship a first-class logs command before asking users to inspect data-dir internals.

Next adjustment:

- Consider supervising/restarting UI from daemon instead of only starting a sibling process.

## 2026-06-16 - Split task view and add transcript deletion

Target:

- Shrink task command view logic and support deleting individual chat messages from UI

Implementation:

- Extracted task projection view types/helpers into packages/workspace/src/commands/task-view.ts; added DELETE /api/transcript/:id and UI delete action

Verification:

- bun run check plus isolated client API transcript delete smoke

Verifier:

- self-check with deterministic command evidence; browser automation blocked by missing local Playwright browser

Drift: closer

Method feedback:

- For UI-visible transcript changes, add a server-level persistence smoke even when browser automation is unavailable

Next adjustment:

- Install Playwright browser cache if richer local UI automation is required

## 2026-06-16 - Harden client API cursor runtime failures

Target:

- Keep the client API alive when Cursor backend settings or credentials are invalid

Implementation:

- Added client-agent runtime preflight, Cursor model discovery via Cursor.models.list, dynamic settings model options, Cursor default model normalization, transcript deletion UI, and task-view split

Verification:

- bun run check; live source client API smoke; compiled sikong-client-api smoke showing turn.error while health remains available

Verifier:

- self-check with deterministic source and compiled binary smoke evidence

Drift: closer

Method feedback:

- Service-facing SDK backends should preflight before entering request loops and expose discovered catalogs through settings rather than hardcoded defaults

Next adjustment:

- Publish beta.10 and verify installer target

## 2026-06-16 - Live agent activity cards

Target:

- Show semantic worker activity instead of raw runtime process cards and use long-task timeouts

Implementation:

- Realtime worker observation writes, Work detail Agent activity cards, runtime diagnostics demotion, 2h orchestration defaults, longer client-agent pass timeout

Verification:

- bun run check; Playwright fallback mobile and desktop render checks against temporary running-worker fixture

Verifier:

- self-check with deterministic tests and rendered smoke evidence

Drift: closer

Method feedback:

- When UI needs to show what agents are doing, persist semantic observations during hooks, not only terminal worker results.

Next adjustment:

- Use the same observation stream for planner/reviewer runs if those need live activity cards.

## 2026-06-17 - Remove stale client agent timeout override

Target:

- Use the workspace client-agent pass timeout defaults from the client UI turn workflow

Implementation:

- Removed explicit passTimeoutMs 60000 and settlementPassTimeoutMs 20000 from packages/client/server/index.ts

Verification:

- bun run check passed

Verifier:

- self-check with deterministic bun run check evidence

Drift: closer

Method feedback:

- When a default appears ineffective, search caller overrides before changing the default again.

Next adjustment:

- none

## 2026-06-17 - Redesign client pending activity card

Target:

- Show client-agent thinking and tool calls instead of static workflow phase cards

Implementation:

- Added client-agent activity streaming, SSE turn.activity events, pending-turn activity state, and an activity timeline renderer

Verification:

- bun run check passed; Playwright with local Chrome verified desktop 1440x1100 and mobile 390x844 against mocked SSE activity events

Verifier:

- self-check with deterministic checks and rendered screenshots

Drift: closer

Method feedback:

- For agent UI progress, render actual normalized loop events before adding decorative workflow steps.

Next adjustment:

- Consider token-level streaming if backend adapters need finer live text updates.

## 2026-06-17 - Separate implementation and test worker duties

Target:

- Sikong soft orchestration prompts should keep implementation workers scoped and move broad verification into explicit test/reviewer work

Implementation:

- Updated worker and lead round-planning prompts in packages/workspace/src/runtime/worker-run.ts and packages/workspace/src/orchestration/tick.ts; added prompt assertions

Verification:

- bun --filter @sikong/workspace typecheck; bun --filter @sikong/workspace test -- presets.test.ts orchestration.test.ts

Verifier:

- self-check with deterministic workspace tests

Drift: closer

Method feedback:

- Implementation workers should not run broad tests by default; lead should create separate test-focused work units when verification is needed

Next adjustment:

- Consider shortening slow client SSE tests with injected timing in a later slice

## 2026-06-17 - Plan-centric Work Detail redesign

Target:

- Make Work Detail readable around plan stages, with rounds nested under stages and expandable worker execution details

Implementation:

- Updated packages/client/src/task-detail.tsx, task-detail-rows.tsx, and task-detail-primitives.tsx

Verification:

- bun --filter @sikong/client typecheck; Playwright local Chrome screenshots at desktop and mobile; bun run check reached fmt:check and failed only on pre-existing unrelated formatting files

Verifier:

- self-check with rendered screenshots

Drift: closer

Method feedback:

- For operational task detail pages, organize by the governing workflow object first, then nest execution records under the exact plan node that owns them.

Next adjustment:

- Clean unrelated formatting backlog before using full bun run check as a green gate

## 2026-06-17 - Work unit detail drawer hierarchy

Target:

- Flatten Work Detail hierarchy so stages contain rounds, rounds show work unit grids, and worker details open in a drawer

Implementation:

- Updated packages/client/src/task-detail.tsx and packages/client/src/task-detail-rows.tsx; retained async section fix in task-detail-primitives.tsx

Verification:

- bun --filter @sikong/client typecheck; Chrome screenshots for desktop drawer and mobile drawer; bun run check reaches fmt:check and fails on existing unrelated formatting files

Verifier:

- self-check with rendered screenshots

Drift: closer

Method feedback:

- Keep workflow pages shallow by using the plan tree for navigation and moving dense execution payloads into a secondary surface.

Next adjustment:

- Consider keyboard focus trapping for the drawer if this becomes a reusable modal primitive

## 2026-06-17 - Client-agent owner view and work-detail audit split

Target:

- Make Chat operate as an owner-level dashboard while Work Detail remains a plan/round/worker audit surface

Implementation:

- Updated chat overview, composer copy, turn progress summary, and compact task detail header in packages/client/src

Verification:

- bun --filter @sikong/client typecheck; Chrome screenshots for desktop chat and mobile work detail; bun run check reaches fmt:check and fails on existing unrelated formatting files

Verifier:

- self-check with rendered screenshots

Drift: closer

Method feedback:

- Separate owner-level control surfaces from execution audit surfaces instead of exposing workspace focus and tool traces as the primary user model.

Next adjustment:

- Consider grouping duplicate cancelled system messages in chat history

## 2026-06-17 - Make sikong logs human-readable

Target:

- Change sikong logs from raw concatenated tails into readable service log output

Implementation:

- Updated cmd/sikong/product.go, cmd/sikong/main.go, product tests, and README with formatted log output and --raw

Verification:

- go test ./cmd/sikong; bun run dev:cli -- logs --lines 6; bun run check reaches fmt:check and fails on existing unrelated formatting files

Verifier:

- self-check with command output

Drift: closer

Method feedback:

- Operational commands should default to human-readable diagnosis and keep raw file dumps behind an explicit flag.

Next adjustment:

- Consider adding timestamps at log write sites so the formatter can show real event time.

## 2026-06-17 - Make sikong logs readable

Target:

- Redesign sikong logs output so default command reads like logs instead of raw concatenated files, while preserving raw access.

Implementation:

- Added formatted log table with time/source/level/component/message, raw mode, formatted follow output, and timestamped managed process log writes for new daemon/UI logs.

Verification:

- go test ./cmd/sikong passed; bun run dev:cli -- logs --lines 8 showed formatted table; bun run check still fails on pre-existing oxfmt issues outside this slice.

Verifier:

- codex

Drift: closer

Method feedback:

- For CLI observability polish, keep raw mode as escape hatch and layer formatting over existing durable logs instead of changing state shape.

Next adjustment:

- Consider adding persisted structured log fields if UI log inspection needs filtering/search later.

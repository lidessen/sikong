/** Observability: read-only views + agent-facing renderers over the stores. */
export {
  renderStatus,
  renderTaskDetail,
  taskDetail,
  workspaceStatus,
  type TaskDetailView,
  type TaskSummary,
  type WorkspaceStatusView,
} from "./inspect";

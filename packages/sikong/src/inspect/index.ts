/** Observability: read-only views + agent-facing renderers over the stores. */
export {
  renderStatus,
  renderTaskDetail,
  renderOverview,
  taskDetail,
  workspaceOverview,
  workspaceStatus,
  type ProjectOverview,
  type TaskDetailView,
  type TaskSummary,
  type WorkerOverview,
  type WorkspaceOverviewView,
  type WorkspaceStatusView,
} from "./inspect";

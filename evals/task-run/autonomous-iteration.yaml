id: autonomous-iteration
max_steps: 48
task: |
  读 development-log 和 git log，了解当前状态。然后往前推进一步——
  让 siko 能更好地处理大型任务（递归分解，最多三层）。

  改完跑 cargo build && cargo test，然后 git add && git commit。
  记到 development-log 里。
expectation: |
  推进了项目。大任务分解能力前进一步。
workspace:
  provider: current-file-system
  read_scope:
    - src/
    - development-log/
  write_scope:
    - src/**
  allow_write: true
